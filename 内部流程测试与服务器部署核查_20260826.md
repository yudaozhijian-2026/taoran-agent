# TAORAN内部流程测试与服务器部署核查

日期：2026-08-26。程序0.7.0，规则TAORAN-Q33-Q34-100-V2，模型glm-5.2，提示词TAORAN-LLM-FACTS-V2.2。

## 结论

当前服务端AI检测、真实模型分析、100分计分及双字段回写代码链路通过内部测试，可进入独立服务器试部署。尚不能宣称简道云页面端到端验收完成：原按钮的过程详细描述、联系人和商机子表云端绑定仍待启用；本轮回写仅模拟，未修改真实业务数据。

服务器已成功只读登录，具备Docker隔离部署条件；尚未上传文件、启动新容器、改Apache、改DNS或重启现有服务。待确认目标目录与独立HTTPS入口后实施，避免把同名旧项目当作本项目覆盖。

## 本轮实测

| 检查 | 结果 | 边界 |
|---|---|---|
| 自动化回归 | 276项通过，代码检查通过 | 包含按钮传递组件、模型异常、评分、回写、版本隔离 |
| 实际本地按钮接口 | 8个案例符合预期；正常响应约1.32—4.19毫秒 | HTTP接口时间，不是简道云页面渲染时间 |
| 临时HTTPS按钮接口 | 8个案例符合预期；约0.70—1.70秒 | 单轮样本，不代表长期稳定或并发压测 |
| 未授权请求 | 本地、公网均401 | 未泄露服务密钥 |
| 真实GLM样例 | 完整、部分达成、缺失3类均通过输出和证据校验 | 分别约11.19、16.84、10.17秒；前检零模型调用 |
| 实际HTTP异步评分 | 入队响应约6.03毫秒，完成约11.09秒 | 合成数据；Q33=50、Q34=50、总分100；无回写目标 |
| 签名Webhook完整代码链 | 验签→映射→真实GLM→计分→模拟API回写成功，约10.53秒 | 独立临时审计库，绝不调用真实简道云写接口 |
| 重复Webhook | 相同事件只模拟写入一次 | 重复投递不重复评价与回写 |
| 非法签名 | 401 | 未进入评分与回写 |
| 简道云字段接口 | 29项匹配、0项未解析 | 只读预览，未更新云端字段或本地映射 |

按钮案例：完整输入、同表单重复检测、下一联系日期早于、同日、修改日期后重检、过程清空、过程漏传、子表格式错误。最后一项正确返回422，不伪装成已检查或未填写；其余均200。六项标准均展示；只返回建议，不含正式分数、知识依据或分析方式。漏传过程与用户清空有不同反馈。

模拟回写严格限定“AI评分”`_widget_1787037882562`及“AI反馈意见”`_widget_1787037882560`，禁止触发回写递归。未提交、更新或覆盖任何真实简道云记录。真实端到端回写仍须在专用测试记录上验收。

复测入口：`deploy/internal_probe.py`。合成结果见`outputs/internal-20260826/`下的local-buttons、public-buttons、live-async及signed-pipeline JSON。运行async或pipeline会调用真实模型；buttons仅调用规则检查。不得把内部探测脚本作为公开服务入口。

## 服务器只读核查

- 已验证现有SSH连接：47.119.191.148；无需用户重新提供密码或模型Key。
- `/TAORAN agent`：空目录，2026-08-26创建，可能为用户指定目录，但名称与“TAORAN原则”不完全一致，待确认。
- `/opt/taoran-agent`：旧TAORAN服务正在运行，systemd服务名`taoran-agent.service`，主进程643。禁止覆盖或重启。
- `/root/DSM_Assessment_Plugin/TAORAN智能体`：已有旧代码，位于40题项目内，不选作新版部署目录。
- 40题Web/API、PostgreSQL、MinIO及知识库相关容器均在运行；40题健康接口正常。实际HTTPS代理为Apache，不能另启一个占80/443端口的代理。
- 可用内存约4.6GB，磁盘剩余约7.6GB；Docker26.1.3、Compose2.27.1已存在，缓存Python3.12基础镜像；8030端口空闲。
- `taoran.yudaozhijian.top`在本轮解析检查中未得到地址；当前DSM证书仅覆盖DSM域名，不能直接拿来冒充TAORAN独立域名证书。
- 40题预留集成仍关闭，未调用或修改40题项目。

## 独立试部署方案

1. 确认目标目录后，采用其下独立release、配置、数据及日志目录；不复制电脑虚拟环境、不迁移或覆盖旧项目数据库。
2. 使用独立Docker项目名`dsm-taoran-v2`，服务名不复用旧`taoran-agent`。单实例运行，仅绑定`127.0.0.1:8030`；独立持久化SQLite，不连接40题数据库。
3. 设置资源上限（初始CPU0.5核、内存768MB）、日志轮转、非root运行、健康检查、自动重启及足够的优雅退出等待。镜像依赖应使用uv.lock固定版本；密钥仅作为受限运行配置提供，不打入镜像或文档。
4. 服务端先使用合成数据检查按钮和真实模型，再开通TAORAN独立HTTPS站点。现有域名配置不覆盖；新增站点先语法检查，再平滑加载，并复查所有原有站点健康。
5. 新入口验证后，再单独切换简道云测试副本的按钮和Webhook，保留原入口以便回退；不切正式拜访表，不修改40题配置。
6. 启用草稿传递插件时，需核验实际字段绑定能力、账户权限及费用提示；任何登录、支付或安全协议由用户处理。没有传来的过程不能被判为已检查。
7. 当前是SQLite＋单进程后台任务，适合受控试运行。进程异常退出后排队任务没有独立持久任务队列自动恢复机制；批量正式使用前需补充恢复策略、备份和容量测试。不能仅因部署成功就宣布生产级高可用。

## 需用户补充的最小信息

1. “TAORAN原则文件夹”是否就是服务器的`/TAORAN agent`？若不是，请给绝对路径。
2. 独立HTTPS域名是否使用`taoran.yudaozhijian.top`？若使用，需要将该域名解析到服务器；现有SSH权限不能证明拥有DNS管理权限。

已有模型、简道云及服务认证配置可沿用，不需要在聊天里重发API Key。确认上述部署目标后，授权范围内的独立部署可继续执行；真实页面测试和插件绑定状态另行如实记录。
