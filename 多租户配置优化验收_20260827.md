# TAORAN多租户配置优化验收

日期：2026-08-27。程序版本：0.8.0。范围：商业化第一项“多客户配置”。

## 结论

已建立版本化租户注册表。每个租户可独立配置启用状态、最多两个轮换访问Key、简道云API Key、Webhook Secret和字段映射路径。
按钮检查、提交后读取、签名Webhook、字段同步、深度评价回写和中文字段名展示均按请求`tenant_id`使用同一租户配置。

本次不修改TAORAN六项标准、Q33/Q34各50分规则、模型提示词、知识快照或40题默认关闭策略。

## 隔离策略

- 注册表中存在的租户以该条记录为唯一来源，缺少配置时直接拒绝对应能力；
- 禁止注册表租户回退到旧全局映射或其他客户密钥；
- 完全未迁移到注册表的现有租户继续使用旧环境变量；
- 租户停用后普通API访问返回403；未知租户或错误Key返回401；
- 注册表最多同时接受两个租户访问Key，支持受控无停机轮换；
- 配置状态接口只返回布尔状态、应用/表单标识和映射版本，不返回密钥或服务器路径。

## 新增材料

- `config/tenant_registry.example.json`：无真实密钥的注册表示例；
- `多租户配置与客户接入.md`：服务器目录、迁移、字段同步、验收和Key轮换步骤；
- `GET /api/v1/tenants/{tenant_id}/configuration`：租户自查配置状态；
- `runtime/`已加入Git忽略，避免误提交真实客户配置；
- 服务器Compose为`/TAORAN agent/runtime`增加只读容器挂载，启用注册表前仍需先准备服务器文件。

## 验证结果

- Ruff静态检查通过；
- 288项自动化测试通过；
- 新增测试覆盖：注册表优先级、旧配置兼容、双Key轮换、租户停用、未知租户拒绝、注册表缺项不回退、两客户字段映射与中文名称隔离、Webhook Secret隔离、配置状态不泄密；
- 0.8.0 wheel和源码包构建成功；
- 使用当前本地`tenant_demo`配置只读验证：健康接口200、版本0.8.0、配置状态接口200、来源`legacy`、密钥未出现在响应中。

## 尚未执行

- 尚未把当前公司`tenant_demo`真实密钥迁移到注册表；
- 尚未部署0.8.0到服务器，线上0.7.0不受影响；
- 尚未创建第二个真实客户简道云账户或修改正式表单；
- 数据库物理隔离、租户额度、可恢复任务队列和客户管理后台属于后续商业化阶段。

服务器启用前必须先备份现有`agent.env`和`field_mapping.json`，创建权限受限的`tenant_registry.json`，以兼容模式和注册表模式分别完成测试副本回归后再切换。
