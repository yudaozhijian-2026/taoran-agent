# 提交前内容缓存与插件关联发布

候选应用版本：0.27.10rc1；唯一镜像及Git标签：0.27.10rc1-content-cache-20260908-1520。
起点为远端main及服务器部署提交73a16c616debdcafa5d5f22a51bcff46c91a7c41。
本任务在独立codex/content-cache-release-20260908-1520分支与worktree中执行，不覆盖旧工作区或其他会话。

## 业务边界

- 保留已读并返回回填；关闭弹窗不回填，不自动提交拜访记录。
- 原样重开复用有效完整缓存或进行中任务，修改参与分析的内容则新建任务。
- 无需业务拜访编码、临时编号或新增隐藏字段。
- 租户/授权/用户分区/表单/配置/规则/知识/模型和已传子表均参与指纹；每个打开批次另有消息标识防止旧弹窗串回填。
- 成功缓存默认30分钟；失败和不完整结果重新检测；保留任务恢复留档。
- 固定任务所用知识快照；知识服务的短缓存更新周期保持原配置。
- Q33/Q34评分、提交后语义观察策略、模型配置、40题和知识库服务均不变。
- 当前仍为单worker去重；不同人员权限须有相应独立授权，客户端user_id本身不构成身份认证。

## 插件同步

仅操作测试副本6a8408b7c5a0d9454090a5bc对应的TAORAN AI检查插件。
前端代码使用integrations/jiandaoyun/quick_check_interactive_frontend.js；后端启动函数使用同目录quick_check_interactive_launcher.js。
保持21项页面字段及子表绑定、snapshot_contract和resText到AI反馈意见的回填不变。
拜访记录编码输入不设必填；移除未发布的draft_session_id请求/返回声明，不新增隐藏字段。
先部署服务，再同步插件代码；旧服务不满足新协议时插件明确报错，不能混用版本后宣称验收通过。

## 发布检查和留档

按Python全量测试、浏览器脚本测试、Ruff、Compose、构建顺序执行。
本地Docker未运行，镜像以锁定依赖的离线wheel在服务器唯一候选目录构建；构建不替换现行容器。
提交推送并快进合并main后，带该Git提交重新构建正式镜像；切换前再次核对main和现行服务版本。
deploy/release_content_cache.py执行preflight/switch/verify，严格检查指定旧镜像ID、部署提交、无在途任务、数据库完整性、配置和其他容器不变。
备份位于/TAORAN agent/backups/before-0.27.10rc1-content-cache-20260908-1520，包含Compose、运行配置、数据库和旧镜像；rollback.sh只回滚TAORAN应用，不自动覆盖新数据。
服务器发布清单位于/TAORAN agent/releases/0.27.10rc1-content-cache-20260908-1520/deployment.json，记录提交、镜像ID、内外网健康、管理页面、配置哈希和回滚路径。

最终实际发布与简道云操作验收结果另行追加；本文不预先声明已发布或已完成真实验收。
