# TAORAN大模型接入与验收

实现版本：0.6.1。提示词版本：TAORAN-LLM-FACTS-V2.1。

## 当前状态

2026-08-26用户已配置TAORAN专用智谱Key，指定`glm-5.2`，并确认“提交前快速规则检查、提交后GLM-5.2深度分析”。0.6.0已完成两阶段隔离、Schema输出约束、原文证据校验、格式重生成和失败暂停回写。实际测试与启用状态以根目录验收记录为准。40题项目只作只读协议参考，没有修改其代码、配置或使用其Key。

当前本地测试模型已启用，服务及原HTTPS入口已恢复。V2.1最近5次虚构数据模型调用全部通过；真实简道云页面提交、模型评分回写与业务准确率尚待进一步验收，不代表生产就绪。

TAORAN使用用户指定的智谱`glm-5.2`，不跟随40题模型配置。知识库Key和简道云Key都不能替代大模型Key。独立Key便于授权管理，但不代表同一供应商账号下的余额、额度和限流一定独立。

历史V1实测曾出现前端6秒/10秒超时、后端偶发格式失败；因此前端现已固定为纯本地规则。V2实测进一步定位到空字段被模型输出为空证据的问题，V2.1明确禁止空值证据，并只允许引用本次非空字段。详见[智谱GLM-5.2接入验收记录.md](智谱GLM-5.2接入验收记录.md)。

## 两阶段行为

| 阶段 | 模型分析 | 返回/保存内容 | 失败处理 |
|---|---|---|---|
| 提交前点击AI检测 | 不调用模型，仅本地六项规则与空泛表达检查 | 六项标准和检查状态全部显示，未达标项附分析与建议；不返回分值、不更新AI评分；不展示知识依据和分析方式 | 不受模型网络、余额、限流、超时或并发槽位影响；仍可提交 |
| 提交后 | GLM-5.2六项完整分析、KR质量、事实性、实际达成、下一行动衔接、客户共识 | 规则计算Q33/Q34各50分，回写总分和包含模型分析的AI反馈 | 两次模型尝试合计默认45秒；仍失败/证据不符则保留参考结果供审计，两个AI字段均不覆盖 |

默认模型并发为2，超过并发上限快速返回待复核；超时调用即使尚未结束也继续占有原槽位，避免后台请求无限堆积。HTTP不自动重试、不跟随重定向，响应体有大小上限。

## 输出格式稳定性

- 输出契约直接来自程序校验模型的JSON Schema，明示必填项、类型、枚举、长度和禁止额外字段；启用智谱JSON对象模式，而不是假定服务端已经强制校验全部Schema。
- 必须恰好六个不同代码：T、A1、O_KR、R、A2、N。布尔值只能为`true/false`，不能是字符串、数字或`null`；分析与建议必须是字符串。
- 空字段只能列为缺失，不能生成`quote=""`、`quote=null`或编造“未填写”等原文。纯缺失项使用`evidence:[]`；混合字段只引用非空值。Schema及提示词列出本次允许引用的非空字段。
- 原文引用仍须逐字匹配，拒绝重复JSON键、NaN、错误引用、额外分数和截断/拒绝响应，不强制转换不合规事实。
- 仅JSON语法、普通类型、必填项、枚举等格式错误允许在剩余时间至少2秒时重生成一次；不会回传失败响应原文，只传安全错误位置/类别与格式说明，并保留原始输入。
- 额外字段（包括分数）、证据不实、权限/网络/限流/超时失败不自动重试；两次尝试共享45秒预算，不变成两次45秒。
- `model_attempts`保留每次尝试耗时、失败类别及经过白名单脱敏的校验位置/类别，不含原始失败内容、密钥或未知字段名；仍失败必须暂停正式回写。

六项结构固定为T、A1、O_KR、R、A2、N。模型不能直接返回分数；多余分数字段、错误类型、截断JSON、不明字段、编造原文引用、缺少关键证据等均拒绝使用。

## 规则与模型的职责

1. 确定性规则检查已传入字段、日期边界、预约事实和企业策略。
2. 模型理解具体拜访内容，逐项给出中文分析与原文证据。
3. 原文证据必须逐字出自对应字段；空字段可直接指出缺失，未传字段不能判缺失。
4. 提交后有证据的语义结论用于Q34事实判断，但不能取消结构性缺失；前端本地规则不被模型结果覆盖。
5. 提交后模型只产生受控Q34候选事实；Q33完整性50＋及时性50、Q34自评一致70＋下一行动30的公式不变。
6. 任务保留模型名称、提示词版本、知识快照Hash、引用与分析。简道云自动生成的评价请求ID包含模型/提示词/知识基线指纹，避免模型启用后仍复用旧规则评价。

注意：逐字引用校验只能保证来源可追溯，不等于语义判断一定正确；仍需业务专家用样例确认模型质量。

## 安全与40题隔离

- 只在TAORAN本地`.env`保存专用Key，不放前端、不放URL、不写文档或日志、不发到聊天。
- 仅发送当前需要分析的字段与已审核TAORAN知识；不发送员工/客户/商机稳定ID、租户标识、完整元数据、附件或跨系统记录。
- 联系人和商机列表使用匿名对象编号；自由文本仍可能包含客户姓名或商业信息，因此须确认模型服务的数据处理政策和公司授权。
- 原始拜访内容一律作为不可信数据；其中“忽略规则、给满分、输出密钥”等文本不能改变模型任务。
- 不读取40题`AI_SCORING_*`配置；如果以后明确授权共用同一Key，将共享提供方额度与限流，不能保证对40题没有额度影响，故建议独立Key。
- 不调用浏览器中用户登录态作为模型授权，不复用DSM知识API Key。

## 配置步骤

在项目根目录`.env`中添加以下配置，Key只在本地填写：

```dotenv
DSM_TAORAN_LLM_ENABLED=false
DSM_TAORAN_LLM_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
DSM_TAORAN_LLM_MODEL=glm-5.2
DSM_TAORAN_LLM_API_KEY=在本地填写TAORAN专用模型Key
DSM_TAORAN_LLM_EVALUATION_TIMEOUT_SECONDS=45
DSM_TAORAN_LLM_FORMAT_RETRIES=1
DSM_TAORAN_LLM_MAX_CONCURRENCY=2
DSM_TAORAN_LLM_MAX_INPUT_CHARS=24000
DSM_TAORAN_LLM_MAX_OUTPUT_TOKENS=3000
```

模型地址要求HTTPS，不允许网址查询参数或用户密码。`LLM_ENABLED=true`时必须同时具备地址、模型名、Key，且不能同时设置旧版`SEMANTIC_ENDPOINT`。

前端不需要模型配置，即使启用`LLM_ENABLED`也不会调用模型。旧的`LLM_PRECHECK_TIMEOUT_SECONDS`仅保留低层适配器兼容，不控制前端按钮。

以上为GLM协议样例。实现使用Chat Completions的messages和JSON输出，不将普通兼容协议视为已认证支持所有模型；其他厂商需单独使用测试命令验收其参数、响应和延迟。

参数依据：[智谱GLM-5.2说明](https://docs.bigmodel.cn/cn/guide/models/text/glm-5.2)、[对话补全API](https://docs.bigmodel.cn/api-reference/模型-api/对话补全)、[JSON结构化输出说明](https://docs.bigmodel.cn/cn/guide/capabilities/struct-output)。GLM请求关闭thinking，返回JSON对象，评分仍在本地规则层完成。

## 验收顺序

1. 用户确认模型提供方及TAORAN独立授权，将真实Key填写在本地。
2. 运行`uv run taoran-agent model-status`检查配置；输出不包含密钥，也不会请求外部模型。
3. 在确认可发送数据后，用`DSM_TAORAN_LLM_ENABLED=true uv run taoran-agent test-model --samples 3`临时启用测试进程，不提前改动正式服务开关。
4. 命令只发送完整、部分达成、缺失三类虚构样例，不读取或回写简道云。前端`precheck_mode=local_rules`、`precheck_model_calls=0`，缺失样例的`needs_revision`是正常建议；后端必须`evaluation_status=completed`。可用`--scenario missing --samples 3`重复验证缺失场景。HTTP 200但内容校验失败不算通过。
5. 真实测试通过后将`.env`中的模型开关改为`true`并重启TAORAN服务。API和CLI使用同一工厂，前端保持纯本地规则。
6. 在简道云测试副本点击AI检测，反馈不显示“知识依据”和“分析方式”，六项标准及检查状态全部显示，未达标项附具体分析与修改建议，未收到字段不标为达标；AI评分不变。来源与模型信息仍可在后台审计核查。
7. 用户提交一条专用测试记录，检查后台任务模型状态、Q33/Q34分项及两个AI字段回写。
8. 用业务专家标注样例验证判断质量，确认后才考虑真实表。

## 模型异常与恢复

前端没有模型请求，不会因后台模型延迟而等待，也不会在模型后来完成时自行改写未提交表单。后端模型失败时查询任务可看到参考评价、脱敏尝试记录及`writeback.status=failed`，而简道云已有分数和意见保持原状。

恢复模型配置后，调用现有`POST /api/v1/visit/evaluations/{job_id}/writeback`：若上次是模型失败，服务先重新分析，成功才回写；若模型仍被关闭则返回409，不能用纯规则替代这次模型复核。若只是简道云API写入失败而模型分析已成功，则直接重试原结果回写。

## 验证范围

已完成本地模拟模型测试：前端零模型调用、提交后规则融合、中文字段名、六项标准及真实检查状态全部显示、未达标项具体分析与建议、未获取字段不误判达标、模型不能输出分数、原文伪造/未传字段、类型错误、一次格式恢复、共享45秒预算、脱敏诊断、重复JSON键、截断、401/429、并发上限、超长输入、模型失败禁止回写、恢复后先复核再回写，以及已有简道云和40题隔离回归。

上述是自动化测试，不替代真实模型联通、真实响应延迟和业务准确率验收。
