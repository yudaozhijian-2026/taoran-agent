# TAORAN本地POC部署记录

> 2026-08-26评分口径更新：当前Q33/Q34各50分、合计100分，程序0.7.0，见《百分制评分调整与验收》。下文历次验收中的200分、旧版本及测试数量为历史记录，不代表新版结果；云端字段绑定待启用状态未变。

更新时间：2026-08-26

## 当前访问地址

- 本地服务：`http://127.0.0.1:8030`，程序版本`0.7.0`；已重启，健康检查200；新评分为Q33/Q34各50分
- 当前临时HTTPS地址：`https://polite-rivers-stick.loca.lt`
- 健康检查：`https://polite-rivers-stick.loca.lt/health`
- AI检测接口：`https://polite-rivers-stick.loca.lt/api/v1/connectors/jiandaoyun/visit/button-check`
- 简道云Webhook：`https://polite-rivers-stick.loca.lt/api/v1/connectors/jiandaoyun/visit/webhook?tenant_id=tenant_demo`
- 原Cloudflare临时地址已失效并从简道云测试副本中替换。

当前地址使用LocalTunnel，仅用于POC。电脑关机、服务停止或隧道重启后，HTTPS地址可能变化；变化后必须同步更新
简道云测试副本的前端事件地址。正式试运行应改为具有固定域名的命名隧道或正式服务器。

最新模型状态：TAORAN专用智谱Key与`glm-5.2`已启用，采用“前端本地规则、提交后模型深评”。历史V2.1提示词五次虚构数据调用通过，详见`智谱GLM-5.2接入验收记录.md`。0.6.5提示词升级为V2.2，本轮仅模拟输出校验，未重测真实GLM或页面提交回写。下方“已完成验证”为历次历史结果，不代表新版端到端验收全部完成。

## 当前运行方式

0.6.5七类修复：完成其他目的补充、三档自评证据、重复内容拦截、总体状态、精确漏传提示、后评六项覆盖和指定结论/事实一致性校验。过程与两个子表传递组件已扩展，本地239项测试、代码检查及构建通过；本地8个接口案例通过。原HTTPS入口可返回0.6.5，测试出现一次非JSON响应后再次4个案例通过，不能称公网稳定。未创建/启用云端插件或保存按钮变更，原按钮漏传仍未闭环；未调用真实GLM、未提交或回写业务记录。见`字段传递与七类修复验收.md`。

0.6.4六项全量展示：按用户最新要求，标题改为“TAORAN六项检查：”，包括达标项在内的六项标准全部显示。达标项显示“达标＋检查标准”，未达标项显示标准、具体分析及修改建议。未获取相关字段标“未检查”，仅部分字段被检查标“待复核”，不把未检查当成达标；适用范围等提示不作为未达标原因。152项自动化测试、代码检查与构建通过，本地服务已重启且HTTPS地址不变。两个入口各验证全部达标、日期异常、重复检测、修正后、未传过程、全部字段未传6个虚构案例，均返回200且含6项标准，公网约0.76—1.09秒；未调用真实大模型或改写简道云业务记录。重新点击AI检测即可刷新意见，无需修改接口配置。此要求替代下列旧版本“仅显示未达标”的历史展示记录，评分与提交后流程保持原样。

0.6.3标准展示：提交前每个未达标项分行显示“检查标准 → 数据分析 → 修改建议”，标准覆盖六项TAORAN，取自本地已审核知识和现行前检规则的中文摘要。达标项及未收到字段的项目仍隐藏；日期异常保留具体日期与原因，N项标准明确自然日必须晚于、同日不达标。不新增判断规则，不修改提交后评分或反馈，不恢复知识依据/分析方式说明行。147项自动化测试、代码检查与分发包构建通过。本地服务已重启，原HTTPS入口保持不变；两个入口各验证日期异常、重复检测、修正后通过3个虚构案例，均返回200；公网接口耗时约0.72—0.83秒。未调用真实大模型或改写简道云记录，实际页面点击由用户继续验收。重新点击AI检测即可刷新当前表单意见，历史保存意见不批量改写。

0.6.2日期异常修复：下一次联系客户时间安排早于或与本次拜访日期相同，按钮正常返回中文异常说明和修改建议，不再因该日期顺序问题返回500。空值单独提示未填写；未收到真实拜访日期时不凭临时默认日期误报；有时区的下一次联系时间按北京时间自然日比较，原始值保留。139项测试通过，原本地服务已重启，HTTPS地址不变。本地与公网各6次虚构样例（早于、重复检测、同日、修正后、UTC跨日、空值）均返回200；公网耗时约0.67—1.07秒。未调用真实大模型、未提交或回写简道云记录，页面实际点击仍由用户验收。详情见`日期异常修复验收记录.md`。

0.6.1展示调整：新生成的提交前/提交后AI反馈均不再显示“知识依据”和“分析方式”说明行；规则、具体分析、评分、失败提示和后台来源审计不变。113项测试通过，本地服务已平滑重启。重新点击AI检测即可刷新当前未提交表单中的意见，不需要提交；历史已保存意见不会自动批量改写。

在项目根目录启动Agent：

```bash
uv run taoran-agent serve --host 127.0.0.1 --port 8030
```

另一个终端使用`npx --no-install localtunnel --port 8030 --local-host 127.0.0.1 --subdomain polite-rivers-stick`申请原HTTPS地址（本机已缓存该工具；若子域名不可用需核对实际输出）。两个进程必须同时运行，本机需要保持联网和唤醒。

## 已完成验证

- HTTPS健康检查返回200；
- 未携带TAORAN服务密钥访问受保护接口返回401；
- 正确密钥访问字段映射返回200；
- Q40预留接口保持关闭；
- 真实简道云记录可以通过HTTPS调用AI检测，返回`can_submit=true`；
- 专用记录`BFJL2026081900001`完成深度评价与双字段回写；
- 回写结果：Q33为100、Q34为0、总分为100；
- 简道云读回AI评分为`100`，AI反馈意见非空；
- 回写字段严格限制为AI评分和AI反馈意见。
- 简道云测试副本的“AI检测”按钮已保存并关联1个启用的TAORAN前端事件；
- 前端事件返回值仅回填AI反馈意见，不在提交前写入AI评分；
- 前端事件字段选择器未提供“过程详细描述”，当前提交前检查不传该项；提交后深度评价继续通过V5 API获取完整记录。
- 专用记录实际点击“AI检测”后，服务返回HTTP 200，AI反馈意见约3.4秒完成回填；
- 实测期间提交按钮始终可用；服务未运行时调用失败也未阻断提交；
- 已兼容简道云空字段字符串`"null"`、`"undefined"`和空字符串，41项自动化测试通过；
- 实测仅验证表单内即时回填，未点击提交，未覆盖记录中已保存的深度评分和反馈。
- 已配置简道云数据推送“TAORAN提交后深度评价”，监听新数据提交和数据修改；
- Webhook Secret仅保存在`.env`，服务按简道云规则校验`X-JDY-Signature`；
- 简道云服务器连接测试已成功，真实签名测试请求返回202；
- 提交后处理采用“快速验签与入队、后台评价与回写”，不占用简道云前端提交等待时间；
- 已通过简道云页面实际修改并提交专用记录，真实`data_update`签名事件成功触发异步评价；
- 首次验收任务与恢复任务均完成并成功回写AI评分和AI反馈意见；
- 验收标记已恢复，权威数据读回确认“过程详细描述”无测试内容残留；
- 最终读回AI评分为`100`，AI反馈非空；AI字段回写未形成递归Webhook；
- V5 API修改在本次环境中未触发数据推送，验收与后续业务流程以简道云页面提交事件为准。
- 已修复同一表单重复点击AI检测时的`check_id`主键冲突；按钮每次点击由服务端生成独立请求ID；
- 公网实测相同内容连续检测、修改内容后再次检测共3次均返回HTTP 200且反馈非空；
- 提交前按钮只更新AI反馈意见，AI评分仍由提交后异步深度评价回写。
- 2026-08-25起，按钮响应不再返回或展示“记录完整度”等数值；按钮接口明确标记`official_score_generated=false`。
- 正式Q33、Q34及总分只由提交后的Webhook后台任务生成，完成后通过简道云V5 API回写AI评分和AI反馈意见。
- 提交前反馈的六项区域标题为“TAORAN六项检查（未达标）：”；达标项隐藏，仅显示未达标项的数据分析和修改建议。
- 已删除AI反馈中的“不阻断表单提交”显示文字；
- 简道云前端事件选择器当前无法选择“过程详细描述”，服务改为仅检查本次请求实际传入的字段，避免把漏传误判为未填写；
- 公网实测只提示已传入但为空的字段，“过程详细描述”不再产生假缺失提示；提交后仍通过V5记录读取完整字段进行深度评分。
- AI反馈已升级为TAORAN六项结构：T客户类型、A预约、O/KR拜访目的与关键结果、R过程事实与结果、A达成评价、N下一步客户行动；提交前和提交后格式均已验证。
- 2026-08-26运行版本为0.5.0，已增加直接大模型适配、逐字原文证据校验和双阶段保护；90项自动化测试通过。
- 首次模型状态检查为`enabled=false`且模型配置未填写；用户随后配置智谱`glm-5.2`及专用Key，V1未通过整体验收。0.6.0改为前端本地规则并完善V2.1输出契约后，已通过虚构样例技术验收并启用测试；简道云模型回写验收仍待进行。
- 2026-08-26已恢复同一LocalTunnel地址，本地与公网`/health`均返回0.5.0；公网按钮接口正常返回建议且`official_score_generated=false`。简道云地址无需修改。
- 新模型接入与本地Key配置见`大模型接入方案.md`，不得将自动化模拟测试当成真实模型调用成功。
- 已接入DSM知识库本地审核快照，提交前引擎升级为`TAORAN-PRECHECK-KB-V1`；55项自动化测试通过。
- 本地真实接口重复点击测试返回HTTP 200，每次生成独立请求ID和检查ID；示例完整记录返回100/100。
- 2026-08-25重新建立HTTPS入口，公网健康检查和AI检测接口返回HTTP 200；
- 简道云测试副本的AI检测前端事件和提交后数据推送均已切换到当前LocalTunnel地址；
- 简道云“服务器连接测试”显示连接成功，Webhook配置保存成功；原Cloudflare备用隧道已停止。

## 下一阶段

- 进入阶段5业务样例校准，准备低、中、高质量脱敏样例和专家评分；
- Quick Tunnel仅用于当前POC，正式试运行前改为固定HTTPS域名和受守护的常驻服务。

## 安全约束

- 简道云API Key、TAORAN服务密钥和Webhook Secret只保存在`.env`，不得写入本文档、代码、样例或日志；
- 字段映射查看接口已增加租户认证；
- Quick Tunnel地址本身不作为认证凭据；
- 测试结束后停止隧道即可立即撤销公网入口；
- 正式试运行前需要增加稳定域名、进程守护、日志轮转、限流和可恢复任务队列。
