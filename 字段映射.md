# TAORAN字段映射

## 当前测试对象

当前接入对象已从正式拜访记录表切换为简道云测试副本`拜访记录录入_AI测评`。字段依据
`references/拜访记录录入_AI测评.xlsx`整理，共47个基础字段。

当前测试副本标识：

- 应用ID：`60fe7ad79ca2d000075dfab1`；
- 表单ID：`6a8408b7c5a0d9454090a5bc`。

2026-08-18已使用简道云V5表单字段接口完成实际字段同步：29个映射项全部匹配，0个未解析。旧正式表的
表单ID和字段ID不再作为活动配置，避免测试误写正式表。

## Q33/Q34输入映射

| Agent字段 | 新副本字段名称 | 用途 |
|---|---|---|
| `visit_date` | 拜访日期 | 拜访日期、下一行动时间边界 |
| `employee_id` | 销售代表（通讯录） | 销售稳定归属；“销售代表”为备用名称 |
| `customer_id` | 客户编号 | 稳定客户关联 |
| `customer_type_ii` | 客户分类II | Q33完整性、Q34客户分层 |
| `visit_method` | 拜访方式 | Q34正式统计范围 |
| `is_appointment` | 是否预约 | Q33完整性、Q34预约标准 |
| `purpose_code` | 拜访目的 | Q33完整性、Q34目的判断 |
| `other_purpose` | 具体其他目的 | 目的为“其他”时补充 |
| `expected_key_result` | 想取得的关键结果 | Q33完整性、Q34 KR质量 |
| `process_description` | 过程详细描述 | Q33完整性、Q34客观事实 |
| `self_assessment` | 评价 | Q33完整性、Q34自评一致性 |
| `next_action_purpose` | 下一次行动目的 | Q33完整性、Q34下一行动 |
| `next_action_other_purpose` | 下一次具体其他目的 | 下一目的为“其他”时补充 |
| `next_action_expected_result` | 下次拜访期望的关键结果 | Q33完整性、规范建议 |
| `next_contact_at` | 下一次联系客户时间安排 | Q33完整性、Q34时间门槛 |
| `actual_start_at` | 实际拜访开始时间 | Q33及时性备用基准 |
| `actual_end_at` | 实际拜访结束时间 | Q33及时性首选基准 |
| `duration_minutes` | 拜访时长 | Q33及时性备用基准 |
| `evidence_ids` | 上传相关文件 | 受控附件引用；截图字段作为备用 |
| `submitted_at` | 简道云`_createTime` | 不可变首次提交时间 |
| `source_record_id` | 简道云`_id` | 评价关联和回写数据ID |

## 关联字段

| Agent对象 | 新副本字段 |
|---|---|
| `participants[]` | 联系人信息 |
| `participants[].contact_id` | 联系人信息.关联数据-主键 |
| `opportunities[]` | 关联商机阶段信息 |
| `opportunities[].opportunity_id` | 关联商机阶段信息.商机编号 |
| `opportunities[].historical_stage` | 关联商机阶段信息.历史商机阶段 |
| `opportunities[].current_stage` | 关联商机阶段信息.最新商机阶段 |
| `visit_record_code` | 拜访记录编码 |
| `manager_comment` | 经理点评 |

“参加拜访人员”是内部/联合拜访人员，不等同于客户联系人稳定ID，因此不映射到`participants[].contact_id`。

## AI回写字段

新副本已经创建两个AI字段：

| Agent输出 | 新副本字段 | 当前内容 |
|---|---|---|
| `total_score` | AI评分 / `_widget_1787037882562` | 实际类型为单行文本；回写字符串形式的TAORAN总分0—200 |
| `ai_opinion` | AI反馈意见 / `_widget_1787037882560` | 实际类型为多行文本；包含Q33/Q34分项、主要问题和修改/辅导建议 |

两个回写字段已确认可用，API Key配置及字段读取鉴权均已通过。为避免影响业务数据，本次未执行真实记录回写。

## 字段ID同步记录

当前活动映射已经完成同步并写入配置。以后测试副本字段发生新增、改名或类型调整时，可重新预览：

配置API Key后，先预览自动匹配结果：

```bash
uv run taoran-agent sync-jiandaoyun-fields --tenant-id tenant_demo
```

确认`unresolved`没有关键字段后再写回活动映射：

```bash
uv run taoran-agent sync-jiandaoyun-fields --tenant-id tenant_demo --apply
```

同步按字段标题匹配，同时递归处理“联系人信息”和“关联商机阶段信息”子表字段；结果保留字段名称作为兼容回退。

## 不能直接映射或需派生的信息

- KR是否具体可衡量、过程是否有客观事实、目的实际达成程度；
- 商机客户对下一行动是否存在明确共识；
- 目标客户周期预约率是否达到50%；
- 客户信息、商机阶段是否在拜访后实际更新；
- 客户反馈、偏差原因和下一行动对象的独立结构字段。

这些信息不得从字段名称或普通描述中臆造。当前已配置真实widget ID；字段名称仅作为读取兼容回退。
