# AI实时分析流式恢复：本地候选修复

日期：2026-09-08。
基准提交：590ca37878a22f97de52d95a0467a78785b7b3b8。
独立分支：codex/restore-live-preview-20260908-side。

## 原因

当前提示词要求直接输出正文，但分段读取还依赖旧的 USER_FEEDBACK 标签；外层又将片段缓存到整次生成结束才发送。两处叠加，使实时分析表现为等待后整段出现。版本化弹窗使用轮询获取已校验结果，轮询间隔还会逐步增加到5秒。

## 修复

- 实时分析正文按模型实际生成的句子逐步发布，不模拟已完成结果的打字效果；兼容旧标签跨片段返回。
- 保留结果格式校验和修复重试；失败片段先清除，再显示重试内容，避免两次生成拼接。
- 弹窗读取生成中的累积内容；正常生成时每秒刷新，网络失败仍退避重试。
- 已完成缓存直接显示全文。缓存复用、关闭弹窗保护、数据版本校验和最终回填逻辑保留。
- 不改评分规则、模型配置及提交后评分流程。

## 验证

- Python自动测试：391项通过。
- 浏览器脚本自动测试：50项通过，包含分段展示、每秒轮询、失败清除、关闭保护和缓存重放。
- Ruff检查通过。
- 上游流使用受控测试模拟，并断言第二段尚未生成时第一段已经发布；没有调用真实模型，也没有提交简道云数据。

## 发布边界

已获授权合入远端主分支并发布，候选应用版本为0.27.10rc2，镜像及Git标签为0.27.10rc2-live-preview-20260908-side。仅发布实时分析修复，不包含其他工作区的后端直接回写修改。实际发布成功以服务器发布目录的deployment.json为准；真实简道云弹窗验收另行执行。每秒轮询会增加单个生成中任务的请求频率，应在商业化并发测试中验证容量。
