# TAORAN多租户配置与客户接入

本文定义不依赖TAORAN业务标准确认的商业化配置底座。租户注册表只负责客户隔离、简道云连接和字段映射，
不改变六项判断、Q33/Q34评分、模型提示词或40题预留接口。

## 配置优先级与兼容性

系统按以下顺序解析每个`tenant_id`：

1. 如果租户存在于版本化租户注册表，该条记录就是唯一配置来源；缺少字段时直接拒绝对应能力，不回退到其他租户或全局值；
2. 只有租户完全不存在于注册表时，才使用现有`DSM_TAORAN_TENANT_KEYS_JSON`、`DSM_TAORAN_JIANDAOYUN_API_KEYS_JSON`、
   `DSM_TAORAN_JIANDAOYUN_WEBHOOK_SECRET`和`DSM_TAORAN_JIANDAOYUN_MAPPING_PATH`兼容配置；
3. 开发环境在完全没有租户认证配置时保留原免认证行为，生产环境拒绝未配置认证的业务请求。

因此当前`tenant_demo`无需立即迁移。新客户应直接使用租户注册表，不能共用客户Key、简道云API Key、
Webhook Secret或字段映射文件。

## 注册表格式

无真实密钥的结构示例见`config/tenant_registry.example.json`：

```json
{
  "version": 1,
  "tenants": {
    "tenant_customer_a": {
      "enabled": true,
      "access_keys": ["当前Key", "轮换期旧Key"],
      "jiandaoyun": {
        "api_key": "该客户独立简道云API Key",
        "webhook_secret": "该客户独立Webhook Secret",
        "mapping_path": "/tenant-config/tenants/tenant_customer_a/field_mapping.json"
      }
    }
  }
}
```

- `version`当前固定为`1`，未知字段或错误版本会被拒绝；
- `enabled=false`立即停用该租户的普通API访问；
- `access_keys`最多同时保留两个Key，用于无停机轮换；
- 每个租户的简道云API Key、Webhook Secret和映射路径独立解析；
- 查询配置状态的接口只返回是否配置、应用ID、表单ID和映射版本，不返回密钥或服务器文件路径。

## 服务器目录

建议生产服务器使用：

```text
/TAORAN agent/
├── runtime/
│   └── agent.env                # 只读运行环境和管理员Key
└── tenant-config/             # 客户管理页专用可写目录
    ├── tenant_registry.json   # 权限600，不进入Git
    └── tenants/
        ├── tenant_demo/
        │   └── field_mapping.json
        └── tenant_customer_a/
            └── field_mapping.json
```

在`agent.env`设置：

```dotenv
DSM_TAORAN_TENANT_REGISTRY_PATH=/tenant-config/tenant_registry.json
```

Compose保持`runtime`只读，只将`tenant-config`挂载为可写。可优先通过`/admin/tenants`网页完成接入；
手工编辑注册表仅作为故障处理途径。详见[客户接入管理页.md](客户接入管理页.md)。

## 新客户接入顺序

1. 在管理页填写客户独立授权的简道云API Key并连接；
2. 系统读取该Key可访问的应用与表单，实施人员填写客户名称并选择目标拜访记录表；
3. 系统自动生成不可复用的`tenant_id`、租户访问Key和Webhook Secret；
4. 系统复制基础字段映射，带入应用ID和表单ID，并按实际表单字段自动同步；
5. 字段全部匹配时立即启用，存在未匹配项时保持停用并给出提示；
6. 启用后检查配置状态接口，完成跨租户拒绝、按钮检查、签名Webhook、提交后深评和白名单回写测试；
7. 仅在测试表通过后迁移到客户正式表。

客户编号、应用ID、表单ID、表单名称、映射路径、启用开关和密钥轮换不是新客户首次接入的手工字段。
注册表仍作为系统配置存储和故障处理底座，日常实施使用网页向导。

配置状态接口：

```text
GET /api/v1/tenants/{tenant_id}/configuration
```

请求必须携带该租户自己的`X-Tenant-Id`和`X-Api-Key`。返回值不包含任何完整Key。

## Key轮换

1. 将新Key放在`access_keys`第一位，旧Key暂时保留第二位；
2. 更新调用方并验证新Key；
3. 从注册表删除旧Key；
4. 重新加载服务并验证旧Key返回401、新Key正常。

简道云API Key和Webhook Secret不支持同时双值；轮换时应安排受控窗口，先更新简道云端再更新注册表并立即验证。

## 安全边界

- `tenant_registry.json`、客户字段映射和真实密钥不得提交Git或打入镜像；
- 本项目已忽略`runtime/`和`tenant-config/`目录；
- 客户之间不得共用映射文件，避免把评价写到另一客户的应用或表单；
- 租户注册表解决配置隔离，不等同于数据库、额度、任务队列和模型成本已经完全产品化；这些仍按商业化后续阶段实施。
