# TAORAN原则评分智能体项目实施方案

> 2026-08-26评分口径更新：当前Q33/Q34各50分、合计100分，程序0.7.0，见《百分制评分调整与验收》。下文历次验收中的200分、旧版本及测试数量为历史记录，不代表新版结果；云端字段绑定待启用状态未变。

版本：V1.0
计划日期：2026-08-19  
实施对象：简道云测试副本`拜访记录录入_AI测评`  
生产原则：先测试副本、后真实表；先非阻断建议、后深度评价；阶段验收未通过不得进入下一阶段。

## 2026-08-26补充：大模型接入

0.6.0已按用户确认分为“提交前本地快速规则检查、提交后GLM-5.2深度分析”。已增加实际Schema契约、空证据约束、一次格式重生成及45秒共享预算、脱敏校验审计；109项测试通过。最新V2.1提示词5次虚构数据模型调用均通过，模型已启用、本地服务和原LocalTunnel入口已恢复。实际简道云页面提交与模型双字段回写验收仍待专用测试记录验证。配置与结果见根目录`大模型接入方案.md`和`智谱GLM-5.2接入验收记录.md`。不得将小样本成功描述为生产稳定性已验证；40题运行不变。

## 一、项目目标

建立一个可独立运行的TAORAN原则评分智能体，实现：

1. 销售填写拜访记录时，点击“AI检测”获得非阻断的规范性修改建议；
2. 拜访记录正式提交后，异步执行Q33、Q34深度评价；
3. Q33与Q34各50分，总分100分（2026-08-26更新）；
4. 将总分和AI反馈通过API回写测试副本；
5. 测试稳定后，按重新映射、影子运行、灰度切换的顺序迁移到真实拜访记录表；
6. 保留40题系统未来主动调用TAORAN的接口，但默认关闭，不改变现有40题评分流程。

## 二、当前基线

### 已完成

- 独立FastAPI服务、CLI、Docker构建、健康检查和接口文档；
- Q33、Q34规则整合为50＋50＝100分；
- 提交前非阻断检查、提交后异步评价、任务查询和回写重试接口；
- 测试副本字段同步：29个映射项全部匹配，未解析项为0；
- AI评分和AI反馈意见回写白名单；
- AI评分文本字段的类型兼容处理；
- 租户隔离、幂等、输入快照Hash、版本与审计基础能力；
- Q40未来调用接口已预留且默认关闭；
- 代码检查、50项自动化测试和安装包构建均通过。

### 当前配置状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 运行环境 | 本地POC | 0.6.0服务及原LocalTunnel HTTPS入口已恢复；不是生产常驻部署 |
| 简道云API Key | 已配置 | 已验证可读取测试副本字段结构 |
| 简道云字段映射 | 已完成 | 29/29匹配，0项未解析 |
| TAORAN服务访问密钥 | 已配置 | 已写入测试副本前端事件Header，与简道云API Key隔离 |
| 简道云Webhook Secret | 已配置 | 仅保存在本地环境变量，用于验证简道云数据推送SHA-1签名 |
| 正式语义模型 | GLM-5.2已启用用于测试 | 仅提交后调用；V2.1五次虚构数据调用通过，真实回写和业务质量仍待验收 |
| Q40集成 | 关闭 | 保持现状，不影响40题评分 |

## 三、阶段总览

| 阶段 | 名称 | 主要结果 | 当前状态 |
|---|---|---|---|
| 0 | 技术基线与字段核验 | 独立Agent、评分规则、字段映射和自动化测试可用 | 已完成 |
| 1 | POC运行准备 | 安全配置、测试数据、服务启动和接口基线可用 | 已完成 |
| 2 | 单条记录端到端验证 | 完成一次读取、检查、深评、查询、回写和重试闭环 | 已完成 |
| 3 | 简道云“AI检测”按钮接入 | 填写页可主动检查，失败或超时不阻断提交 | 已完成 |
| 4 | 提交后深度评价接入 | 保存后异步评价并回写两个AI字段 | 已完成 |
| 5 | 业务样例校准 | 低、中、高质量样例与专家判断达到约定一致性 | 进行中：工具完成，待业务样例与专家标注 |
| 6 | 性能、安全与故障验证 | 响应时间、并发、降级、幂等、审计通过 | 未开始 |
| 7 | 测试副本试运行 | 真实用户小范围使用并完成问题闭环 | 未开始 |
| 8 | 正式表迁移与灰度上线 | 不影响现有流程地迁移到真实拜访记录表 | 未开始 |
| 9 | Q40可选集成 | 仅在40题系统主动调用时启用 | 暂不实施 |

## 四、分阶段实施内容

### 阶段0：技术基线与字段核验——已完成

实施内容：

- 固化TAORAN核心Agent文件、评分规则、架构和字段映射；
- 核验测试副本应用ID、表单ID和widget ID；
- 验证Q33、Q34各50分及100分汇总；
- 验证仅允许回写`AI评分`和`AI反馈意见`；
- 验证Q40预留接口默认关闭。

验收结果：当前45项自动化测试通过，构建成功，字段映射29/29匹配。

### 阶段1：POC运行准备——已完成

实施任务：

1. 为`tenant_demo`生成并配置独立的TAORAN服务访问密钥；
2. 确定测试部署地址，要求简道云可通过HTTPS访问；
3. 在测试副本准备至少3条脱敏测试记录：完整、部分缺失、明显不规范各1条；
4. 记录其中1条专用回写测试记录的`data_id`；
5. 启动服务并验证健康检查、接口认证、字段映射读取和本地持久化；
6. 确认日志中不输出API Key、客户敏感原文或完整请求头；
7. 备份测试副本当前结构和测试记录。

验收标准：

- HTTPS服务稳定可访问；
- 无密钥请求被拒绝，正确密钥请求可通过；
- 三类测试记录可被字段适配器正确解析；
- Q40接口仍返回“未启用”；
- 不修改正式拜访记录表。

阶段门禁：需要确定HTTPS部署位置，并准备一条允许覆盖AI评分/反馈的专用测试记录。

### 阶段2：单条记录端到端验证

实施任务：

1. 使用真实测试副本记录调用AI检测接口；
2. 核对字段解析、缺失项、规范建议和`can_submit=true`；
3. 使用同一记录发起提交后深度评价；
4. 查询异步任务结果，核对Q33、Q34、总分和意见；
5. 首次执行真实API回写，仅更新两个AI字段；
6. 人工确认简道云显示内容和格式；
7. 重复请求验证幂等，模拟失败验证回写重试；
8. 验证`is_start_trigger=false`不会引起递归评价。

验收标准：

- 测试记录成功写入0—100分文本和完整反馈（新版真实回写待现场验收）；
- 原始销售字段没有任何变化；
- 相同请求不重复评分或重复产生副作用；
- 写回失败可追踪、可重试；
- 全链路保留request ID、job ID、trace ID和规则版本。

回退方式：关闭简道云事件调用，保留Agent结果；必要时人工清空测试记录中的两个AI字段。

### 阶段3：简道云“AI检测”按钮接入

实施任务：

1. 在测试副本配置“AI检测”前端事件；
2. 发送当前未提交表单数据、表单修订号、租户ID和服务密钥；
3. 将缺失项、规范性建议和检查时间展示给用户；
4. 用户修改后允许再次检测，每次使用新的request ID；
5. 配置8—12秒目标响应时间和超时提示；
6. 确认按钮、公式和流程均不设置提交阻断条件。

验收标准：

- 完整、缺失、格式错误三类表单都能给出可理解结果；
- `can_submit`始终为`true`；
- 接口失败、超时或返回`review`时仍能保存表单；
- AI建议不自动覆盖销售填写内容；
- 95%的按钮请求在12秒内返回基础结果。

回退方式：停用或隐藏“AI检测”按钮，不影响原有保存与提交功能。

### 阶段4：提交后深度评价与自动回写

实施任务：

1. 在测试副本保存后事件中调用深度评价接口；
2. 传递`_id`、`_createTime`、完整表单数据和回写目标；
3. 简道云接收202后立即结束等待，由Agent异步执行；
4. 配置评价状态查询、失败记录和人工重试流程；
5. 排除AI字段自身变化触发重复评价；
6. 核对首次提交时间不被更新时间替代。

当前进展（2026-08-19）：

- 已增加简道云签名Webhook入口和V5单条记录读取能力；
- Webhook按简道云规则校验`X-JDY-Signature`，并限制为测试副本应用和表单；
- Webhook在校验、入队后立即返回202，深度评价和回写在后台执行；
- 已配置并保存数据推送“TAORAN提交后深度评价”，监听新数据提交和数据修改；
- 服务器连接测试已收到简道云真实签名请求并返回202，简道云显示连接成功；
- 已完成专用记录的权威数据读取、异步评价、双字段回写和重复事件幂等验证；
- 已通过简道云页面实际修改并提交专用测试记录，完整验证签名业务事件、异步评价和双字段回写；
- 验收标记已通过第二次页面提交恢复，权威数据读回确认原始业务字段无测试内容残留；
- 两次页面提交分别生成独立任务，均完成评价和回写，AI字段回写未再次触发Webhook；
- 最终专用记录读回结果为Q33 100、Q34 0、总分100，AI反馈非空。

验收标准：

- 新建和必要的业务修改可触发评价；
- AI字段回写不会形成事件循环；
- 回写仅限两个白名单字段；
- 失败不会丢失评分结果，恢复后可重试；
- 前端提交耗时不受深度评价耗时影响。

回退方式：关闭提交后事件；Agent与历史评价记录保留，不删除业务数据。

### 阶段5：业务样例校准

实施任务：

1. 由业务专家选取不少于30条脱敏样例，低、中、高质量各不少于10条；
2. 专家独立标注Q33、Q34分项、关键问题和建议；
3. 对比规则结果与专家结果，登记差异原因；
4. 只调整有规则依据的阈值、词表和语义提示；
5. 将确认样例固化为黄金回归集；
6. 确认规则版本、生效时间和变更记录。

验收标准：

- 确定性Q33计算与人工复核一致率100%；
- Q34关键结论与专家判断一致率达到双方确认目标，建议初始门槛不低于85%；
- 严重漏判率为0；
- 每个扣分项均能追溯到输入证据和规则版本。

阶段门禁：业务负责人签字确认黄金样例及允许上线的误差范围。

当前进展（2026-08-19）：

- 已新增独立校准命令`taoran-agent calibrate`；
- 已支持Q33/Q34逐条误差、一致率、建议一致率、平均绝对误差和严重漏判统计；
- 已设置默认门槛：总样例不少于30条、每档不少于10条、Q33一致率100%、Q34在10分容差内一致率不低于85%、严重漏判为0；
- 校准数据模型强制拒绝`writeback_target`，不会修改简道云数据；
- 已提供脱敏结构示例和根目录《业务样例校准.md》；
- 1条演示样例已成功运行，工具正确判定样本数量与档位分布未达到业务签字门槛；
- 自动化测试增至45项；
- 仍需业务方确认样例来源、专家、Q34容差、严重漏判定义、质量分档方式和匿名稳定ID保留策略。

### AI反馈字段名称修正（2026-08-19）

- 提交前修改建议和提交后AI意见统一使用简道云活动映射中的实际中文字段名；
- 英文内部字段路径仅保留在结构化接口、审计和计算过程中；
- 已覆盖普通字段、系统字段及“关联商机阶段信息”等子表字段；
- 未配置名称的字段对业务用户显示为“相关字段”，不会泄露英文内部名称；
- 运行服务实测显示“想取得的关键结果”“过程详细描述”“下一次联系客户时间安排”，英文路径检查结果为0；
- 自动化测试增至48项。

### AI检测重复点击修复（2026-08-19）

- 实测无反馈的直接原因为重复快照生成相同`check_id`，触发SQLite主键冲突并返回500；
- `check_id`现已纳入每次点击的独立`request_id`，同一表单内容重复检查不会冲突；
- 简道云按钮入口忽略前端复用的旧请求ID，每次点击均由服务端生成新请求ID；
- 已验证相同内容连续检查、修改内容后再次检查均返回HTTP 200且反馈非空；
- AI检测仍只回填“AI反馈意见”，不在提交前写入正式“AI评分”；正式分数继续由提交后深度评价回写；
- 自动化测试增至49项。

### AI检测提示文字与漏传字段修复（2026-08-19）

- 已删除修改建议和反馈结尾中的“不阻断表单提交”类用户可见文字；非阻断策略仍由接口和流程配置保证；
- 最近真实请求审计确认：“想取得的关键结果”已传入，但“过程详细描述”没有随简道云前端事件传入；
- 简道云前端事件的“插入字段”选择器中搜索不到“过程详细描述”，因此未强行保存无效配置；
- 连接器现在记录本次请求实际传入的字段集合，提交前检查只对“已传入但值为空”的字段给出补充建议；
- 未被前端事件传入的字段不再被误判为用户未填写，提交后的完整深度评价仍使用V5权威记录检查全部字段；
- 公网实测反馈不再包含“过程详细描述”误报及“不阻断”文字；
- 自动化测试增至50项。

### TAORAN六项反馈模型升级（2026-08-19）

- AI反馈意见统一调整为`T客户类型—A预约—O/KR拜访目的与关键结果—R过程事实与结果—A达成评价—N下一步客户行动`六项结构；
- 提交前只输出检查结论、六项规范检查和优先修改建议，不产生或展示数值评分；
- 提交后结合Q33、Q34和200分总分，输出六项深度判断及优先改进建议；
- “过程详细描述”等未被前端事件传入的字段显示为“本次未获取、提交后核验”，不会误报为未填写；
- 所有业务可见字段继续使用简道云实际中文字段名；
- 公网接口和本地提交后样例均已验证新格式，50项自动化测试通过。

### TAORAN知识库驱动检查引擎升级（2026-08-25）

- 通过DSM知识API完整检索TAORAN相关知识，纳入`DSM-BS-000`和`DSM-BS-01-07`两条有效记录；
- 以已批准/已确认知识为上位依据，本地既有规则作为字段映射、确定性校验和Q33/Q34实现依据；
- 新建`TAORAN-PRECHECK-KB-V1`检查引擎，提交前按六项结构返回状态、证据、问题和建议；
- 第二个A统一解释为“达成评价”，N统一为“下一步客户行动”，O统一使用`O/KR`；
- 运行时读取已审核的本地知识快照，远程知识同步与前端AI检测解耦；
- Q33、Q34各100分和总分200分的提交后正式评分规则保持不变，Q40预留接口保持默认关闭。

### 阶段6：性能、安全与故障验证

实施任务：

1. 执行按钮接口延迟、并发和持续运行测试；
2. 验证模型超时、简道云超时、网络中断和服务重启；
3. 验证租户隔离、密钥轮换、请求限流和字段白名单；
4. 验证敏感信息脱敏、日志保留周期和审计查询；
5. 评估SQLite和进程内后台任务的容量边界；
6. 若进入多实例或正式生产，迁移到PostgreSQL和可恢复任务队列后重新测试。

验收标准：

- 按钮接口P95不超过12秒，超时不阻断提交；
- 深度任务失败可恢复且不丢任务；
- 跨租户访问、非白名单回写和无效签名全部被拒绝；
- 目标并发下无数据错写、重复写和任务串线；
- 监控能识别错误率、延迟、积压和回写失败。

### 阶段7：测试副本试运行

实施任务：

1. 选择5—10名销售和1—2名经理，试运行1—2周；
2. 记录按钮使用率、建议采纳率、提交成功率、评价耗时和回写成功率；
3. 每日处理技术故障，每周复核评分与建议质量；
4. 汇总误判、字段缺失、表达不清和流程体验问题；
5. 完成高优先级问题修复并再次回归。

建议验收指标：

- 原表单流程受影响事件为0；
- 提交成功率不低于原流程；
- 自动回写成功率不低于99%；
- 严重评分错误为0；
- 业务负责人确认可以进入正式表影子运行。

### 阶段8：正式表迁移与灰度上线

实施任务：

1. 重新读取真实拜访记录表结构，不复制测试副本widget ID；
2. 生成正式表字段映射差异清单并人工确认；
3. 在正式表新增或确认AI评分、AI反馈字段；
4. 先以影子模式运行，只计算不回写、不影响提交；
5. 小范围启用按钮，再小范围启用自动回写；
6. 观察至少一个约定周期后逐步扩大范围；
7. 保留一键关闭按钮事件和提交后事件的回退开关。

验收标准：

- 正式表字段100%确认，无测试副本ID残留；
- 影子结果与测试副本规则一致；
- 灰度期间无提交阻断、无错记录回写、无递归触发；
- 运维、业务和数据负责人共同确认正式上线。

### 阶段9：Q40可选集成——暂不实施

只有40题系统明确提出主动调用需求时才实施：

- 单独评审接口契约和规则版本兼容性；
- 使用独立服务密钥；
- 先只读调用周期事实，再评估批量补评；
- 不允许Q40批量调用触发简道云回写；
- 未设置`DSM_TAORAN_ENABLE_Q40_INTEGRATION=true`时始终关闭。

## 五、阶段执行与变更规则

1. 每次只允许一个阶段处于“进行中”；
2. 每阶段完成后更新本文件的状态、验收证据、遗留问题和回退点；
3. 涉及简道云真实数据写入前，必须明确测试记录ID和允许写入字段；
4. 涉及真实拜访记录表前，必须重新同步字段并人工确认差异；
5. 评分规则变化必须升级规则版本，不覆盖历史结果；
6. API Key和服务密钥只进入环境变量或密钥管理系统，不进入Markdown、样例、日志和代码仓库；
7. POC期间不改动40题项目；Q40接口保持默认关闭；
8. 任一阶段验收失败时停留在当前阶段，按回退方式恢复，不带问题进入下一阶段。

## 六、当前执行点

阶段4“提交后深度评价接入”已完成，阶段5“业务样例校准”正在执行。阶段1至阶段5当前执行结果如下：

1. TAORAN服务访问密钥已配置；
2. 本地Agent已通过Cloudflare Quick Tunnel提供临时HTTPS地址；
3. 唯一专用记录已识别并完成真实深度评价；
4. AI评分和AI反馈意见双字段回写成功；
5. 简道云“AI检测”按钮已关联并保存1个TAORAN前端事件；按钮字段校验通过；
6. 前端事件已配置POST地址、租户认证Header、19个可直接选择的Body字段，以及`AI反馈意见 ← $.feedback_text`返回值；
7. “过程详细描述”虽然存在于表单和V5字段映射中，但未出现在简道云前端事件的字段选择器中，因此未用非等价字段替代；提交后深度评价仍可通过V5 API读取该字段；
8. 专用测试记录已完成按钮实测：接口返回HTTP 200，AI反馈意见约3.4秒回填，提交按钮保持可用；
9. 实测发现简道云会把空日期发送为字符串`"null"`，连接器已统一规范化空字符串、`"null"`和`"undefined"`；
10. 本次实测没有提交表单，因此没有覆盖专用记录原有的深度评分和反馈。
11. 已实现简道云签名Webhook、V5单记录读取和附件证据适配，自动化测试增至41项；
12. Webhook Secret已安全写入本地环境配置，未写入代码、文档或日志；
13. 简道云数据推送“TAORAN提交后深度评价”已保存，监听`data_create`和`data_update`；
14. 简道云服务器连接测试成功，TAORAN服务收到带`nonce`、`timestamp`及有效签名的请求并在2秒预算内返回202；
15. 经用户确认后，已在简道云页面修改并提交专用测试记录，真实`data_update`签名事件返回202；
16. 任务`job_ce42cbb38e1b2c2b8a44`完成评价和双字段回写，回写状态为`succeeded`；
17. 随后通过页面恢复原始“过程详细描述”，恢复事件任务`job_a54596148e08c7abad47`同样完成并成功回写；
18. 权威数据读回确认过程描述已恢复到原长度351且无验收标记，AI评分为`100`，AI反馈非空；
19. 恢复回写后未出现新增Webhook请求，确认AI字段回写没有形成事件循环；
20. 实测发现V5 API修改即使传入`is_start_trigger=true`也未触发当前数据推送，阶段4验收以简道云页面真实提交事件为准。

### 2026-08-19执行记录

- [x] 复核项目结构、接口、评分规则和Q40隔离边界；
- [x] 复核简道云API Key已配置，字段映射29/29匹配；
- [x] 代码规范检查通过；
- [x] 41项自动化测试通过；
- [x] 源码包和安装包构建成功；
- [x] 确认Q40集成保持关闭；
- [x] 配置TAORAN服务访问密钥；
- [x] 确定HTTPS测试部署地址并完成公网连通；
- [x] 识别专用回写记录；
- [x] 完成阶段1外部连通性和认证验收；
- [x] 完成专用记录深度评价和双字段真实回写；
- [x] 修复真实简道云日期、成员对象和`createTime`格式适配；
- [x] 将字段映射查看接口纳入租户认证；
- [x] 经用户确认后，将TAORAN服务密钥写入简道云前端事件Header；
- [x] 完成可用Body字段和返回值设置并保存AI检测按钮；
- [x] 经用户确认业务测试数据传输后，使用专用记录实际点击AI检测按钮；
- [x] 验证AI反馈意见约3.4秒回填且提交不阻断；
- [x] 验证服务不可用时不阻断提交，并修复空日期字符串导致的500错误；
- [x] 进入阶段4，完成签名Webhook、异步深度评价触发代码和双字段回写能力；
- [x] 在简道云测试副本保存数据推送，监听新建和修改事件；
- [x] 通过简道云服务器连接测试，确认签名校验和202快速响应；
- [x] 经用户确认后触发真实测试记录修改并完成阶段4最终验收；
- [x] 恢复专用记录原始业务内容并验证无测试标记残留；
- [x] 验证两次任务均成功回写且AI回写不形成递归事件；
- [x] 进入阶段5并完成校准数据结构、计算工具、命令行入口和演示样例；
- [x] 校准工具禁止配置简道云回写目标；
- [x] 45项自动化测试通过；
- [x] 修正AI反馈字段名称，业务可见文本全部使用简道云实际中文字段名；
- [x] 48项自动化测试通过；
- [x] 修复同一表单重复点击AI检测的检查ID冲突；
- [x] 验证相同内容和修改后内容均可重复检查并返回最新反馈；
- [x] 49项自动化测试通过；
- [x] 删除AI反馈中的“不阻断表单提交”显示文字；
- [x] 修复简道云前端事件漏传字段导致的“过程详细描述”假缺失；
- [x] 50项自动化测试通过；
- [x] 将提交前和提交后AI反馈升级为TAORAN六项判断模型；
- [ ] 业务方确认校准口径并准备不少于30条脱敏样例与专家评分基准。
