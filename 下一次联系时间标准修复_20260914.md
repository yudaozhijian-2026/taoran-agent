# 下一次联系时间标准修复（本地候选）

基线：刷新 origin/main（f973663）后，在独立 codex/customer-contact-policy-20260914 工作副本中快进合入生产用量监控提交 c279c46。原脏工作区未变更。未部署、未推送、未回写真实记录。

## 已实现

- 后端输入始终包含程序计算的客户类型时间标准，即使联系日期为空：目标客户跨北京时间自然月，潜力客户跨自然季度，商机客户无固定跨期门槛、需客户共识。均要求日期晚于拜访日期。
- 行动内容合理性、日期、客户共识分别判断，评分引擎继续组合它们；Q33/Q34权重与评分代码未修改。
- 模型分析、六项建议及建议依据套错周期时，进入现有有上限的局部修复，不再被 observe-only 吞掉；不扩大修复到无关部分。
- 日期是唯一缺口而行动逻辑被判false时，只允许局部重新判断 next_action_logic_ok；其他事实冻结。若行动内容另有不足，必须给出对应字段依据，不能机械改成true。
- 最终反馈增加防漏校验，错误仍残留则不发布错误意见。修复耗尽不会将错误结果当作成功结果。
- 后端策略版本更新，前端响应设置和前端输入契约未改。

## 验证

- Python全量回归504项通过（含22项新增用例），改动文件Ruff通过。
- 两条真实记录仅提取必要业务输入及错误反馈留作本地回放，移除了员工、记录和客户编码等标识。原错误均能被一致性校验命中。
- 覆盖三类客户空日期、同日、早于拜访、同月、跨月、同季度跨月、跨季度、跨年、UTC转北京时间边界，以及局部修复不改变其他事实。
- 两条记录原始 next_contact_at 均为空；不因仅修正跨期文字而恢复下一行动15分。

## 尚未进行

未调用收费模型重新生成这两条记录的意见，未做简道云真实操作验收；当前为原数据/原输出回放与模拟模型接口回归，不代表真实模型成功率或耗时已经验收。
发布前应完成真实GLM-5.2候选复测、核对生产是否有新提交，按发布流程合并并备份。另行排查历史 WRITEBACK_VERIFY_MISMATCH，不在本次规则修复范围。
