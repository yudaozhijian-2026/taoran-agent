# 0.27.4rc1-v46-20260907

基于 origin/main `0e510b4`，在独立 `codex/taoran-front-p0-advice-v46` worktree 合入。

- P0：优先分配必需的目标分析契约；过程分段仅作无损结构归组，不因同属 C_PROCESS 而要求整篇重写。
- P0：可定位的业务错误只请求对应片段，合并后仍执行原有证据、事实及独立语义校验；未知范围保留原受控重试路径。新增重试类型与结构处理元数据。
- 意见修复：计划联系日期不能表述为双方已约定；真实约定保留。过程质量与合同目标支持程度分开，局部纠正建议及展示标签。
- 修复只作用于意见，不新增拦截、模型调用或正式评分规则，不修改源记录、简道云账号、知识库或40题。
- 保留 rc3 已完成的页面恢复、Preview/Final 隔离、提交后策略及回写保护。
- 切换前端意见缓存命名空间；不删除业务数据或清空数据库。

## 验证与限制

新增测试全部使用合成记录和模拟模型，无客户原始数据入库到 Git。覆盖分段结构、局部修改边界、再次语义核验、流式分片修复、真实约定、建议归属、非实验分支及评分不变。正式前端提示词与 `0e510b4` 的合成输入基线哈希对比，防止基线目录缺失时误用当前代码造成假通过。

此前同一批15条候选测评：最终生成15/15首次通过，无整篇/局部模型重试；本轮真实输出未触发本地修复分支，该分支另有历史输出回放及合成测试。记录质量分15条均保持一致。Final中位数14.275→19.899秒、P95 36.114→40.367秒、超过30秒1/15→4/15；Preview首次可见中位数2.097→2.088秒。此为API轮询观测，非公网实际绘制耗时，不宣称整体提速。第1、4条主体归属仍有歧义，需继续评估。

发布检查：236项Python测试、23项浏览器交互逻辑测试、全库Ruff、Compose依次通过；只替换 dsm-taoran-v2-agent。部署时记录来源提交、镜像摘要、数据库完整性/计数、配置哈希、其他容器ID与启动时间，留存数据库一致性备份、原镜像与回滚Compose。无数据库迁移；常规回滚只切回原镜像，不覆盖上线后的业务数据。
