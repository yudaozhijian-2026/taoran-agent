# 前端意见设置恢复 V4.6

应用版本：0.27.6rc1。恢复基线：225e089529aaad616a3543ddc5f63c5e911810a3（0.27.4rc1-v46-20260907，用户指定的 15 条测评版本）。

恢复弹窗实时意见和最终反馈的 V4.6 提示、生成结构、证据校验与局部修复规则，重新分别生成和显示 Preview、Final。代码隔离在 front_v46，当前 llm.py、评分、提交后反馈及回写代码保持原样。

保留基础检查、后台异步生成、同版本任务复用、权限、断线查询、重启恢复、版本校验和旧版本回写保护。新任务标记前端政策版本，不能复用恢复前的生成缓存或旧政策任务；已有任务仍可按原凭证查询。基础检查不能冒充模型结果。V4.6 前端语义校验和独立复核也随意见设置恢复，失败仍可查询和恢复；不修改后台语义观察政策。

此版本恢复两个意见生成分支，因此模型调用量与单分支 V4.7 不同。没有重新测试或回写业务数据，也不清除历史数据。

发布仅替换 dsm-taoran-v2-agent。部署前备份 Compose、运行配置、数据库及旧镜像；记录其他容器状态、运行配置哈希和数据库摘要，部署后逐项比较。回滚仅切换旧镜像，不用旧数据库覆盖上线后的记录。
