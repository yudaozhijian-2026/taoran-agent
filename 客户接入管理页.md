# TAORAN 客户接入管理页

## 用途

管理页把租户注册表转换为两步式中文接入向导。实施人员只需填写简道云API Key、客户名称，
再从该Key实际授权的应用和表单中选择目标拜访记录表。服务端会按以下顺序处理：

1. 使用API Key读取其授权范围内的简道云应用和表单，不允许手工填写越权ID。
2. 自动生成唯一客户编号，并带入已选择的应用ID、表单ID和表单名称。
3. 测试该API Key是否能读取指定简道云表单。
4. 根据字段中文名称和子表结构生成客户独立字段映射。
5. 为新客户生成TAORAN访问Key和简道云推送签名密钥。
6. 原子写入租户注册表，写入成功后立即重载，不需要重启服务。
7. 字段未全部匹配时保存为停用状态，避免错误配置进入真实流程。

## 页面操作

1. 使用TAORAN管理员Key进入管理页；
2. 填写客户独立的简道云API Key，点击“连接简道云”；
3. 连接成功后填写客户名称，并选择授权应用和拜访记录表单；
4. 点击“完成配置”，保存一次性返回的TAORAN访问Key和推送签名密钥。

同一个“简道云应用ID＋表单ID”只能绑定一个客户。已经接入的表单会在选择框中显示所属客户并禁止再次选择；
即使绕过网页直接请求接口，服务端也会拒绝重复写入。升级前形成的历史重复记录不会直接删除密钥或配置，
但会按最早接入记录在客户列表中归并为一条，避免管理页重复显示。

配置结果会继续显示“下一步：完成该客户的简道云部署”，包括字段确认、密钥保存、AI检测插件、提交后数据推送和
真实测试五个步骤，并生成可直接复制的AI检测接口、系统客户编号和提交后推送地址。离开配置结果后，也可以在
“已接入客户”列表点击“查看下一步部署”重新打开指引；出于安全原因，已关闭页面的一次性密钥不会重新显示。

## 待确认字段处理

字段未全部自动匹配时，配置结果会逐项显示：标准中文字段名、所属表单位置、内部映射路径和
同一表单范围内可选择的实际字段。管理员可以：

1. 在下拉框中选择对应的简道云实际字段，点击“保存手动映射并重新检查”；
2. 如果简道云没有合适字段，先新增或修改字段名称，再点击“重新读取表单字段”；
3. 离开页面后，可以从“已接入客户”列表点击“查看并确认字段”继续处理。

同一个简道云字段不能重复映射；系统会校验字段类型和子表范围。所有必需字段确认完成后，
客户自动启用；仍有待确认项时继续保持停用。评分及回写字段不支持直接忽略。

如果首次选择了错误表单，可以在配置结果点击“返回重新选择表单”，或在客户列表点击“更换表单”。
系统使用该客户已经保存的简道云授权重新读取应用和表单，并默认定位到当前表单。重新保存只更新目标表单和
字段映射，原系统客户编号、TAORAN访问Key和推送签名密钥保持不变；点击“取消重新选择”则不修改原配置。

页面不要求手工填写客户编号、应用ID、表单ID、表单名称、映射路径、启用状态或密钥生成选项。

## 启用

在服务器`/TAORAN agent/runtime/agent.env`中增加：

```text
DSM_TAORAN_ADMIN_ENABLED=true
DSM_TAORAN_ADMIN_API_KEY=<使用密码管理器生成的独立高强度Key>
DSM_TAORAN_TENANT_REGISTRY_PATH=/tenant-config/tenant_registry.json
DSM_TAORAN_ADMIN_AUDIT_PATH=/data/tenant_admin_audit.jsonl
```

服务器上单独建立`/TAORAN agent/tenant-config`目录，仅容器运行用户和受权管理员可读写。
部署包中的Apache配置已为`/admin/`增加反向代理，不需要额外建立网站。
重启新版服务后访问：

```text
https://taoran.yudaozhijian.top/admin/tenants
```

## 安全边界

- 管理员Key与客户访问Key、简道云API Key完全分离。
- 网页不使用Cookie、`localStorage`或`sessionStorage`保存管理员Key。
- 列表接口不返回简道云API Key、签名密钥或TAORAN访问Key。
- 新密钥只在生成后返回一次，实施人员必须当场存入密码管理器。
- 每次新增、更新和密钥轮换写入不含密钥的JSONL审计记录。
- 公网生产建议在Nginx/Cloudflare再限制公司IP或VPN，不要只依赖管理员Key。

## 失败处理

简道云连接失败时不写入注册表。字段部分未匹配时会保存映射和待确认项，但客户保持停用；
完善简道云字段或完成手动映射后，使用页面上的重新检查功能即可继续原客户配置，不会生成重复客户编号。
