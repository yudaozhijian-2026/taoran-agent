# 简道云插件接入验收

日期：2026-08-26。范围仅为“拜访记录录入_AI测评”测试副本。

## 0.11.2三反馈改造说明（2026-08-27）

- 保留原“TAORAN AI检测”单按钮，一次后端请求同时返回`rule_feedback_text`、`knowledge_feedback_text`、`model_feedback_text`。
- 三个输出字段已通过简道云V5接口核对：`AI反馈意见（规则反馈）`为原字段改名，字段ID不变；新增`AI反馈意见（知识库反馈）`和`AI反馈意见（大模型反馈）`。
- 规则反馈继续执行原有本地六项规则；知识库反馈实时调用DSM知识API后交给智谱生成，不回退本地快照；大模型反馈直接调用智谱且不注入知识库内容。
- 三条分支相互隔离，知识库或大模型失败只影响对应反馈。旧`feedback_text`继续等于规则反馈，兼容旧调用方。
- 本文后续“已验证”内容是2026-08-26旧版单反馈的历史验收证据，不代表0.11.2已经部署或完成真实页面验收；0.11.2需在新服务、知识API Key和云端插件代码全部更新后另行实测。

## 已保存的配置

- 应用：`60fe7ad79ca2d000075dfab1`；表单：`6a8408b7c5a0d9454090a5bc`。
- 插件：TAORAN拜访草稿检查；ID：`6a8eb85dbf85ae2ed88e820f`；已启用。
- 函数：拜访草稿TAORAN检查；ID：`func.6a8eb85dbf85ae2ed88e81fe`；Node.js 20，使用内置axios。
- 原有“TAORAN AI检测”单个动作改为插件，未增加并行旧请求；保存表单后重新加载，确认插件及映射持久存在。
- 服务：`https://taoran.yudaozhijian.top/api/v1/connectors/jiandaoyun/visit/button-check`。
- 通用参数：endpoint_url、tenant_id、api_key。沿用原TAORAN服务认证，密钥不保存到本文或代码。
- 19项文本、2项真正子表单输入；对应字段清单见《过程详细描述传参接入》。子表均使用“按子表单赋值”。
- 联系人只传稳定编号；商机保留每行商机编号、历史阶段、最新阶段。通讯录成员JSON文本先提取稳定编号，不将JSON全文或姓名作为员工ID。
- 仅将feedback_text填到“AI反馈意见”`_widget_1787037882560`；不绑定“AI评分”。check_status仅作为调试返回项。

## 已验证

1. 插件代码云端调试成功，合成多行正文及2+2行子表返回正常中文检查意见。
2. 前端事件调试按实际表单字段填写，服务器审计确认正文换行保留；R项达标。清空后新检查R项未达标，不读历史记录。
3. 前端事件调试两行联系人ID分别为PLUGIN-UI-C1/C2；商机分别为PLUGIN-UI-O1（P2→P3）、PLUGIN-UI-O2（P3→P4）。审计`chk_577a5bd30e58934c1b0b`确认逐行对应完整。
4. 拜访日期及下一次联系日期填写后正确传入；人员格式修正后的审计`chk_518394022d5ca6f46790`确认员工标识不再是JSON文本。
5. 保存并重新加载后，实际数据列表“添加”打开未提交草稿，填写两行合成正文，实点AI检测，页面AI反馈意见成功显示。审计`chk_ed2c7087796631ddcf3e`，北京时间18:19:36，正文34字且保留换行，R达标。
6. 同一实际草稿修改为“感觉沟通不错，应该有机会。”后再次检测，审计`chk_f2426ec05b21a44b692b`，18:19:52，R未达标并要求区分事实与判断。
7. 全项目自动测试282项通过，其中插件42项通过；仅有既有测试依赖弃用警告。
8. 实际草稿再清空正文并检测，审计`chk_d473622eefa107f9a912`，18:22:14，正文为空，页面更新为“过程结果缺失”。结束后确认离开，丢弃仅本次合成草稿；原有草稿未操作。

以上测试没有点击“提交”或“保存草稿”，没有修改既有拜访数据。实际填报入口测试与设计器预览不同：设计器预览点击未观察到反馈，不以预览作为成功证据。

## 保留边界

- 空日期可能被简道云完全省略，当前仍保守显示“接口未获取”；已填写日期正常传入。不能把空日期边界宣称已全部解决，也不使用今天或旧记录补日期。代码仅将实际存在但为undefined的日期属性转null，完全缺失仍保持未知。
- 下一次行动对象的独立字段来源、联系人角色等业务标准仍待公司确认；本次联系人传入不代表已经证明下一次行动对象。
- 未对真实业务准确率、所有条件显隐组合、附件上传或移动端作全面验收；当前验证是技术接入与典型草稿检测。
- 插件内HTTP请求超时12秒，错误时返回中文失败意见；不保证每次固定耗时。平台外层故障仍需管理员查看插件运行日志。
- 本次未改提交后Webhook、Q33/Q34各50分、GLM-5.2深评及两AI字段回写流程；没有重启服务器或影响40题、正式表、知识库与其他项目。

## 费用与维护

只读确认云币余额100.00，支付开关原本开启；本次未充值、未变更企业支付开关。现场说明为基础执行10000次1云币、第三方依赖10000次2云币，具体以平台结算为准。未上传自定义依赖。

代码位于`integrations/jiandaoyun/button_check.js`，云端代码需与此文件保持一致。新表复制接入时必须重新核验21项字段绑定、子表逐行结构和输出字段，不能只复制URL。

恢复旧动作时，只将同一个动作切回原自定义POST地址及原18项输入、租户认证、`$.feedback_text`输出；不得同时保留两动作。旧方式仍漏传过程多行/子表，不可当成完整检查使用。密钥从受控配置中读取，不写入恢复文档。
