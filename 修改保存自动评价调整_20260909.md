# 修改保存自动评价与反馈标题调整

发布候选：1.0.2-auto-update-20260909。实际部署状态以服务器同名 releases 目录中的 deployment.json 为准；不批量改写历史记录。

回归验证：465项Python测试、48项前端交互测试通过，Ruff及Compose校验通过。发布使用独立工作区，构建后合并远端主分支，只替换TAORAN容器。部署脚本为 deploy/release_auto_update.py，包含版本防覆盖、空闲检查、配置/数据库备份和上线验证。

- 首次提交：维持自动深度分析、评分回写。
- 修改保存：签名校验、租户/目标表约束后，按当前表单映射规范化TAORAN输入，与同租户、同表单、同记录的最新分析输入比对。
- 相关输入发生变化或无历史分析：发起新任务并回写；同输入正在排队/运行则不重复调用。
- 无关字段、AI输出及平台更新时间变化：不新建分析；之前失败也不会因无关修改不断重试。
- 每次更新使用独立请求标识，再在记录级锁下比对，允许A→B→A等业务内容恢复时重新评价。
- 前端手动点击、当前页面分析、内容缓存和弹窗生命周期保持不变。去除前后端意见开头的“【AI反馈意见】”，并覆盖旧缓存前端回填及旧结果后端重试回写；不删除正文中的引用。
- 不改变Q33/Q34各50分、总分100分，不增加提交阻断。
- 正式、测试仍按各自租户和表单隔离；服务更新部署后对两者生效。

边界：本次没有恢复回写前版本拦截，多个不同版本任务仍按现行“完成即回写”规则运行。旧任务后完成可能覆盖新任务结果的既有风险仍在；如需保证永远以最新保存版本为准，应另行恢复版本保护，不应与当前授权下的回写规则混为一谈。

上线后验收：相关字段修改保存自动回写；无关字段修改不分析；AI回写不循环；不同租户记录互不复用；前端及后端回填无重复标题。历史已存意见不作批量清理，只在后续正常回填/回写时使用新格式。
