# TAORAN业务样例校准方案

版本：V2.0  
日期：2026-08-26  
阶段：阶段5——业务样例校准

## 一、校准目标

使用经过脱敏、由业务专家独立标注的真实拜访样例，对比TAORAN智能体与专家在Q33、Q34和有效性建议上的差异。校准只运行本地评价，不配置简道云回写目标，不修改测试副本或正式表。

## 二、样例要求

- 总数不少于30条；
- 低、中、高质量各不少于10条；
- 每条保留完整的Q33、Q34必要输入，但客户名称、人员姓名、电话、地址等必须脱敏；
- 每条由专家标注Q33分、Q34分、有效性建议和关键问题编码；
- 专家标注时不查看智能体结果，避免结果相互影响；
- 样例应覆盖不同客户类型、拜访方式、是否预约、达成状态和下一行动质量。

## 三、默认验收门槛

| 指标 | 默认门槛 |
|---|---:|
| Q33与专家评分一致率 | 100%，默认容差0.005分 |
| Q34与专家评分一致率 | 不低于85%，默认容差5分 |
| 低、中、高质量样例 | 每档不少于10条 |
| 严重漏判 | 0条 |

“严重漏判”当前定义为：专家建议为`no`但智能体建议为`yes`，或者专家标记的关键问题编码未出现在智能体问题列表中。该定义需要业务负责人最终确认。

## 四、运行方式

校准数据使用JSON文件，结构见`examples/calibration_dataset.example.json`。运行：

```bash
uv run taoran-agent calibrate examples/calibration_dataset.example.json
uv run taoran-agent calibrate calibration_dataset.json --output data/calibration_report.json
```

报告包含逐条误差、Q33/Q34一致率、有效性建议一致率、平均绝对误差、严重漏判、质量档位分布和是否达到业务签字门槛。校准程序会拒绝任何带`writeback_target`的样例。

## 五、需要业务确认的信息

1. 30条样例的来源：由业务方提供脱敏数据，还是授权从指定简道云测试/历史数据范围只读抽取后脱敏；
2. 负责独立标注和最终签字的业务专家；
3. Q34“5分容差、85%一致率”是否作为首轮正式门槛；
4. 严重漏判是否采用本文件第三节定义，是否还有必须纳入的业务红线；
5. 低、中、高质量由专家先验分组，还是按专家总分区间自动分组；
6. 是否允许样例中保留匿名化的稳定客户ID，以验证跨记录事实关联。

以上六项未确认前，可以验证校准工具，但不能宣布阶段5业务验收通过，也不调整正式评分规则。

评分数据集必须声明`rule_version: TAORAN-Q33-Q34-100-V2`，专家Q33/Q34标签范围均为0—50；缺少版本或旧版标签不得静默换算。容差随量纲等比减半，一致率门槛不变，业务签字仍待完成。
