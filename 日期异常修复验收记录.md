# 下一次联系客户时间异常修复验收记录

> 2026-08-26评分口径更新：当前Q33/Q34各50分、合计100分，程序0.7.0，见《百分制评分调整与验收》。下文历次验收中的200分、旧版本及测试数量为历史记录，不代表新版结果；云端字段绑定待启用状态未变。

日期：2026-08-26  
版本：0.6.2

## 问题与修复范围

简道云AI检测请求中，当下一次联系客户时间早于本次拜访日期时，原输入模型直接抛出校验异常，按钮接口返回500，导致前端收不到反馈。

现将该日期顺序问题留给规则检查处理，不再在输入解析时拒绝记录。保留原始日期，不自动修改拜访记录，也不将异常值置空冒充未填写。本次针对下一次联系时间与拜访日期的业务顺序异常，不表示所有输入格式错误或其他接口故障均已覆盖。

## 前端展示

在“TAORAN六项检查（未达标）：”的“N｜下一步客户行动”中显示：

> 下一次联系客户时间安排：异常。异常说明：当前安排日期（2026-08-24）早于本次拜访日期（2026-08-25），不满足下一次联系日期须晚于本次拜访日期的要求。

修改建议：核对“下一次联系客户时间安排”与“拜访日期”，按实际拜访及后续联系计划修正；下一次联系日期应晚于本次拜访日期，修改后可再次点击AI检测。

- 早于本次拜访日期：说明“早于”，列出两个日期。
- 同一自然日：说明日期“相同”，按原有晚于自然日规则提示异常，即使同日晚些时候也不算达标。
- 已传入但为空：显示“未填写”，与已填写的日期异常区分。
- 未收到下一次联系时间：不推断用户未填写。
- 未收到真实拜访日期或使用临时默认日期：只提示补充拜访日期，不凭默认日期推断下一次联系时间异常。
- 按Asia/Shanghai业务时区比较自然日；无时区输入按业务本地时间理解。有时区原始值不改写，避免UTC传输造成跨日误判。
- 改成有效日期后再次检测，异常提示消失；继续支持重复检测。
- 不生成提交前正式评分、不阻断提交；继续隐藏知识依据、分析方式和“不阻断”说明文字。

## 提交后边界

前检不抛错不等于业务达标。提交后的Q34下一行动日期门槛仍生效，早于或同日时该项为0/30；大模型即使给出合格事实也不能绕过日期规则。客户类型对应的跨月、跨季度规则保留，并与前检使用相同业务时区。Q33/Q34权重、每题100分和总分200分不变。

## 验证结果

- 139项自动化测试通过；代码检查和差异格式检查通过；0.6.2分发包构建成功。
- 日期异常覆盖标准检查接口、简道云扁平按钮请求、结构化请求和实际widget字段封装。
- 覆盖早于、同日、晚于、空值、未收到字段、默认日期、UTC跨日、无时区日期、重复点击及修正后重新检测。
- 模拟模型配置下确认按钮仍不调用模型；模拟模型将异常日期判合格时，确定性评分仍拒绝下一行动得分。
- 隔离数据库验证提交后异步评价：异常日期可正常完成评价，虚构完整样例Q33=100、Q34=70、总分170，无回写目标，回写跳过。此为本地规则测试，不代表真实GLM评分或简道云回写验收。
- 重启前确认仅有8条已完成任务，没有待执行/执行中任务；仅重启TAORAN服务。
- 本地与原HTTPS健康检查均返回200及0.6.2版本。
- 两个入口各6次虚构按钮请求均返回200：早于、重复早于、同日、修正后、UTC次日、空值。公网按钮耗时668—1071毫秒，本地1.19—2.65毫秒；这是接口耗时，不是简道云页面点击到显示的实测耗时。
- 每次按钮请求返回独立检查ID，正式评分标记为false，提交允许且不阻断。
- 未调用真实大模型、未提交/修改/回写简道云业务记录；仅在本地留下虚构检查审计。40题项目与配置未改动，其预留接入继续关闭。

## 用户验收

在简道云测试副本当前表单重新点击AI检测即可，无需更改接口地址。确认下一次联系日期早于或同于本次拜访时出现具体异常说明；按真实计划修正后再点，确认该异常消失。本轮未代替用户进行浏览器页面实际点击验收，也未批量更新历史已保存反馈。
