# 提交前确认独立测试环境实施记录

## 2026-09-16：只读预检

- 已刷新 origin/main，提交 f943315c84bfea4d07e229219c9f77288cee317f。
- 独立分支 codex/submit-confirm-isolated-20260916，独立 worktree；未修改原脏工作区源码。
- 服务器生产版本 1.0.5-contact-facts-20260914；镜像 revision 与 origin/main 相同。
- 正式容器 dsm-taoran-v2-agent，重启计数0，健康状态正常。
- 镜像ID sha256:2cbce612502978e217b660532af7f5a3e33b755a6f5373360cb5a9722281b9cb。
- 服务器可用内存约4364MiB，根分区剩余8.1GiB；具备小规模逐条测试的初步资源条件，尚未做负载验收。
- 端口8031未出现在当前监听列表中；最终启用前须重新检查。
- 未创建服务器测试容器，未调整代理、证书、防火墙、正式环境配置或简道云绑定。

## 拟定隔离边界（待入口确认）

- 独立域名建议 taoran-test.yudaozhijian.top，A记录指向47.119.191.148；需用户确认并配置DNS。
- 独立容器/Compose项目、数据库、任务、缓存、日志；仅本机监听，HTTPS代理接入。
- 只允许测试表 6a8408b7c5a0d9454090a5bc，拒绝正式表和其他表的写入。
- 不复制正式数据库；仅提取测试租户必需配置并按最小权限配置，敏感配置不进入Git或报告。
- 保留正式服务不变，真实模型逐条测试；共享模型账号仍共享并发资源。
- 测试前须确认测试表Webhook密钥和回写授权可用，禁用其他服务的通知副作用。

## 额外观察（未作诊断或修复）

公开health显示post_writeback累计123次失败、0次成功，而post_generation累计123次成功。统计窗口、失败原因和受影响租户尚未确认，不能据此判断所有正式记录都失败，也不能将健康状态等同业务回写正常。独立测试验收须单独检查实际回写。

## 后续顺序

1. 用户确认测试域名并完成解析。
2. 准备测试专用交互与表单隔离约束，完成本地测试和构建。
3. 检查资源/基线未变化后建立独立测试实例和HTTPS入口；不重启正式TAORAN容器。
4. 仅将测试插件和测试表事件连接到测试实例，保留模拟配置恢复路径。
5. 逐条验证提交确认、关闭续接、缓存、改动后重检及提交后评分回写。
6. 正式切换另行确认，不在建立测试环境时执行。

## 测试域名核验

2026-09-16 用户完成DNS后，公共DNS223.5.5.5及服务器解析均返回 taoran-test.yudaozhijian.top → 47.119.191.148。尚未签发测试证书或启用测试站点。

只读核对注册表发现三个既有测试表租户（tenant_fe64542a2b07、tenant_433327714475、tenant_c60bcd808957），均指向测试表；正式租户tenant_taoran_prod指向正式表。未改动任何租户或密钥。隔离环境不能直接复制完整注册表或挂载生产runtime目录。下一步需落实测试专用简道云授权，避免默认沿用授权范围未经确认的生产凭据。密钥应由用户在受控配置中录入，不进入聊天和版本库。

## 2026-09-16 独立服务已启动

- 用户在受控文件填写独立简道云Key，读取测试表字段接口HTTP200，返回72个字段。没有将Key写入Git、镜像或日志。
- 候选版本1.0.6rc1，应用源码提交4d5fd5b，分支已推送；未合并正式main。
- 本地535项Python测试及49项交互逻辑测试通过；变更Python文件Ruff检查通过；Compose配置及服务器镜像构建通过。
- 根目录 /TAORAN agent/isolated-submit-test-20260916；容器taoran-submit-test-agent；本机127.0.0.1:8031。
- 镜像taoran-submit-test:1.0.6rc1-20260916；镜像ID以服务器deployment-audit.json为准。
- 仅载入tenant_433327714475的测试表映射，替换为测试专用Key；独立数据库、任务、缓存、日志；关闭管理写入、飞书通知及40题集成；模型并发2、前端保留1，仅逐条验收。
- 新增两个独立Apache站点，仅平滑加载代理配置；没有重启任何既有应用容器。未修改既有站点文件。
- HTTPS https://taoran-test.yudaozhijian.top/health 返回200，release_version=1.0.6rc1；管理入口403。独立证书有效至2026-12-15。
- 首次启动被基础镜像的production环境变量阻止，已在测试Compose显式覆盖并仅重建测试容器；最终重启计数0。首次证书签发发生外部验证连接超时，确认HTTP可达后单次重试成功。
- 尚未更改简道云插件、测试表提交事件或数据推送；仍为独立模拟流程。真实AI端到端及真实回写尚未验收，不能称为全部完成。
- 回退仅停止测试容器、停用两个新增测试站点；不触碰正式容器/站点/数据库。服务器审计文件deployment-audit.json保存镜像ID、容器状态、配置哈希及数据库完整性。
