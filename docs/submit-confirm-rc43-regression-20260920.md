# 提交前确认隔离测试回归记录（2026-09-20）

## 范围

- 仅验证隔离测试服务、测试插件和“拜访记录录入_AI测评”表。
- 未修改正式试点服务、正式插件、正式拜访记录表或评分权重。
- 隔离服务版本：`1.0.6rc43`。
- 隔离镜像：`taoran-submit-test:1.0.6rc43-20260920`。

## 三项问题及处理结果

1. **商机客户日期误判**
   - 案例：拜访日期 2026-09-20，下一次联系日期 2026-09-28。
   - 结果：前端明确识别日期晚于拜访日期，不再将同一自然月作为商机客户的未达标项，也未给出跨月建议。

2. **已提交记录点击“返回修改”仍被保存**
   - 处理：测试插件在返回修改或直接关闭时中止本次简道云提交，不再以成功结果结束提交事件。
   - 实测：商机案例点击“返回修改”后，记录更新时间仍为 `2026-09-20 16:01:20`，没有产生保存更新。
   - 已知交互：简道云会显示“前端事件执行失败”的系统提示。这是当前利用插件失败来终止提交的副作用；数据安全目标已达到，但正式推广前仍需确认是否接受该提示。

3. **弹窗等待后自动关闭或自动提交**
   - 测试：空“下一次联系客户时间安排”的潜力客户案例，生成意见后保持弹窗 76 秒不操作。
   - 结果：弹窗未关闭、记录未自动提交；简道云仅显示“提交时间过长”提示。76 秒后点击“确认提交”仍可正常完成提交。

## 三条真实数据回归

| 案例 | 覆盖场景 | 前端结果 | 提交/回写结果 |
| --- | --- | --- | --- |
| 潜力客户_模拟客户121__其他目的_达到目的 | 下一次联系时间为空；弹窗停留超过60秒 | 分析和改善建议完整；76秒保持弹窗 | 确认后正常提交；AI评分65、深度意见存在 |
| 商机客户_苏州银行股份有限公司苏州分行_10:26_收集信息_部分达到目的 | 同月但晚于拜访日期；返回修改 | 未错误要求跨月；日期说明与页面数据一致 | 返回修改后未保存，更新时间不变 |
| 目标客户_测试客户006__收集信息_部分达到目的 | 跨自然月；确认提交 | 正确识别10月15日晚于9月20日，未对未来承诺作过期判断 | 确认提交成功，记录更新时间更新为2026-09-20 16:58:14 |

## 自动测试

- Python：641 项通过。
- 浏览器与测试插件：72 项通过。
- 测试插件新增覆盖：返回修改、直接关闭、旧弹窗消息、无插件自动关闭时限。
- Ruff 全项目扫描仍报告 4 个既有导入排序问题，均不在本次两个改动文件中；本次未顺带修改无关文件。

## 服务器核对

- `taoran-submit-test-agent`：运行正常，重启次数 0。
- 健康接口：`status=ok`，预热完成，任务并发占用为 0。
- 正式容器 `dsm-taoran-v2-agent`：运行正常，重启次数 0，未被本次操作修改或重启。
