# TAORAN 提交前确认 rc41 验收记录

## 范围

- 只发布隔离测试服务 `taoran-submit-test-agent`和测试插件。
- 测试表：拜访记录录入_AI测评。
- 正式试点服务、正式插件、40题项目和其他服务未修改。

## 发布证据

- 版本：`1.0.6rc41`
- Git 提交：`63f65bd`
- Git 标签：`submit-test-1.0.6rc41-20260920`
- 镜像：`taoran-submit-test:1.0.6rc41-20260920`
- 镜像 ID：`sha256:d024f730af2457887f59ff15e516720cac6a2d3dbaf0a6f42607cdebc8045f6d`
- 回滚备份：`/TAORAN agent/isolated-submit-test-20260916/backups/before-1.0.6rc41-20260920`

## 修复内容

1. 兼容知识库紧凑返回结构，缺少 `content_hash`和 `updated_at`时在服务端生成稳定快照元数据。
2. “本次拜访分析”和“AI改善建议”绑定同一份知识快照和统一判断底稿。
3. 没有需改善的记录返回 `no_change_needed`，不再被当作“改善建议生成失败”。
4. 提交前检查在简道云60秒上限前安全返回“返回修改”，避免超时后复用旧确认状态。
5. 修复测试插件更新时旧代码与新代码被拼接的发布问题，最终插件仅有一份完整前端代码。

## 自动化与发布检查

- Python：638 项通过。
- 前端交互：81 项通过。
- Ruff：通过。
- Compose 配置：通过。
- 服务器镜像构建：通过。
- 测试服务内外网健康接口：正常，知识快照来源为 `remote_api`。

## 真实全流程验收

测试标识：`rc41全流程验收20260920`。

### 新建记录

- 点击原生“提交”按钮后自动打开 AI 提交前确认窗口。
- “本次拜访分析”和“AI改善建议”均完整生成，未出现字段误绑定或知识快照失效。
- 点击“确认提交”后表单正常保存，数据总数增加1条。
- 前端意见已写入；约20秒后，后端深度意见与75分自动回写。

### 修改已提交记录

- 修改了过程事实、下一步期望结果和下次联系日期。
- 再次点击“提交”后，前端使用最新内容重新分析，两个意见模块均完整生成。
- 点击“确认提交”后修改保存成功；约15秒后，后端基于修改后内容重新生成并回写深度意见，评分为75分。

### 后端监控

- 本轮 `post_submit`：2次，成功2次，失败0次。
- 深度评价最长：19.629秒。
- 回写最长：1.195秒。
- 端到端最长：20.914秒。

## 正式环境核对

- 正式健康接口仍为 `release_version=1.0.9`且返回 `status=ok`。
- 本次未更新、重启或修改正式 TAORAN 服务及其他项目。

## 双案例全流程复测

复测时间：2026-09-20。仅使用隔离测试服务和“拜访记录录入_AI测评”测试表。

### 案例一：目标客户

- 拜访记录编码：`BFJL2026092000341`。
- 新建记录完成提交前 AI 检查、确认提交、提交后深度评价和自动回写；评分为75分。
- 修改过程事实、下一步期望结果后再次提交，提交前检查使用最新数据，提交后深度意见再次更新；评分为75分。
- 最新深度意见：原目标要求确认设备数量、预算范围和审批时间三项信息。过程显示预算审批已通过、采购负责人已明确、设备清单已取得，但最终采购数量尚未确认仅约定后续跟进，属部分达到。销售自评部分达到与独立判断一致。下一步目的和期望结果承接本次未完成事项，联系日期跨自然月符合目标客户标准。
- 前端检查观察耗时：新建约28.5秒，修改约30.3秒（包含浏览器交互与页面渲染时间，不等同于纯模型耗时）。

### 案例二：商机客户

- 拜访记录编码：`BFJL2026092000342`。
- 新建记录完成提交前 AI 检查、确认提交、提交后深度评价和自动回写；评分为75分。
- 修改过程事实、下一步期望结果后再次提交，提交前检查使用最新数据，提交后深度意见再次更新；评分为75分。
- 最新深度意见：原目标要求确认技术认可及进入内部评审的条件。过程显示客户确认材料完整并同意9月28日组织内部评审，进入评审条件已达成；技术认可仅有材料完整的间接体现，无客户明确技术认可表述，故部分达成，与自评一致。下一步行动承接本次评审安排，客户已同意组织评审形成共识，联系时间已填写。
- 前端检查观察耗时：新建约39.2秒，修改约26.7秒（包含浏览器交互与页面渲染时间，不等同于纯模型耗时）。

### 服务侧结果

- 隔离服务 `post_submit`累计7次，成功7次，失败0次。
- 本轮观测到的提交后端到端最长耗时为24.092秒。
- 正式服务仍为 `1.0.9`，累计指标未因本轮测试发生变化。

### 发现的问题

1. 商机客户新建记录的前端意见曾出现“下一次联系日期不晚于本次拜访日期且处于同一自然月”，但实际日期为2026-09-28、拜访日期为2026-09-20。流程未失败，但这是一处日期关系文案误判。
2. 修改已提交记录时，第一次误选2026-09-02并点击“返回修改”后，测试记录的数据日志和前端意见出现更新痕迹。该行为说明“返回修改”对已提交记录可能存在提前写入副作用，需要在正式升级前单独确认其产品预期。

## 下一次联系客户时间为空案例

复测时间：2026-09-20。仅使用隔离测试服务和“拜访记录录入_AI测评”测试表。

- 拜访记录编码：`BFJL2026092000343`。
- 客户类型：潜力客户。
- 下一次联系客户时间安排：留空。
- 本次拜访事实：客户确认六台设备安装位置与电源准备均已完成并发送现场照片，销售逐项核对无遗漏；双方尚未约定下一次联系日期。

### 提交前快速检查

- 服务端快速检查任务 `qc_63816273821247f49552ee5c3be8a70f`正常完成，耗时约17.7秒。
- “本次拜访分析”正确识别原目标已达成、自评与事实一致，以及下一次联系日期未填写。
- “AI改善建议”正常生成：`过程已记录双方尚未约定下一次联系日期，请补充一个具体的下次联系时间。`
- 空日期未造成字段读取异常、模型失败或意见缺失。

### 提交后深度评价与回写

- 评价任务：`job_831db9387fa70d78e4bc`，状态为完成。
- 评价结果：Q33为18.75分、Q34为46.25分，总分65分。
- 深度意见正确指出：下一步目的和期望结果承接本次事实，但下一次联系客户时间安排未填写；潜力客户应补充跨自然季度的联系日期。
- 深度评价约20.34秒，简道云回写约1.42秒，端到端约21.81秒。
- AI评分和AI反馈意见均已成功回写测试表，未发生后端生成或回写失败。

### 本轮发现的交互问题

- 前端意见生成过程中，弹窗约53秒后自动消失，记录在未点击弹窗“确认提交”的情况下完成提交。
- 因此，本案例的前端和后端意见内容均正常生成，提交后回写也正常，但“必须由用户确认后才继续提交”的交互控制未通过验收。
- 该现象与空日期判断规则无关，更可能发生在简道云提交事件等待、插件回调或页面状态衔接环节；正式升级前需要继续定位并修复。
- 正式TAORAN服务及正式拜访记录表未修改、未重启。
