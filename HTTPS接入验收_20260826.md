# TAORAN独立HTTPS接入验收

日期：2026-08-26；版本：0.7.0；目录：服务器`/TAORAN agent`。

## 当前结论

独立HTTPS已开通，公网快速规则检查及真实GLM-5.2异步评分通过。简道云测试副本的按钮、提交后通知还没有切换到新域名；本次未写入真实业务记录。

| 用途 | 地址 |
| --- | --- |
| 服务状态 | `https://taoran.yudaozhijian.top/health` |
| 提交前AI检测 | `https://taoran.yudaozhijian.top/api/v1/connectors/jiandaoyun/visit/button-check` |
| 提交后签名通知 | `https://taoran.yudaozhijian.top/api/v1/connectors/jiandaoyun/visit/webhook?tenant_id=tenant_demo` |

按钮保持现有专用租户认证，密钥仅放请求头，不放网址或前端公开代码。提交后通知保持简道云签名校验，不改为无认证接口。正式评分仍是Q33、Q34各50分，总分100分；前端只给意见，不给正式分数。

## 本轮结果

- DNS：阿里云新增`taoran` A记录→`47.119.191.148`，默认线路、TTL 600秒。`dns29.hichina.com`、`dns30.hichina.com`、223.5.5.5和1.1.1.1均已核验。
- TLS：独立证书的域名仅为`taoran.yudaozhijian.top`，有效期至2026-11-24 16:01:20（北京时间）；外网请求正常验证证书，没有忽略证书校验。
- 健康检查200；HTTP转HTTPS为301；根路径302到健康检查页。本项目是API服务，不是独立业务操作网页。
- 公网8种按钮测试通过：完整输入、同表单重检、日期早于/同日、日期改正、过程清空/漏传、错误子表格式。正常请求展示六项标准、不给正式分数；日期异常返回修改建议；字段漏传明确说明接口未获取。
- 本轮公网按钮耗时约49–163毫秒（含错误格式422），是当前测试网络下的接口耗时，不是简道云页面总耗时或长期性能保证。
- 公网异步评分约140毫秒接受任务，12.78秒完成真实GLM-5.2分析，Q33=50、Q34=50、总分100；使用合成数据，不设置回写目标，回写状态为skipped。
- 无密钥按钮401，错误签名Webhook401；`/docs`、`/redoc`、`/openapi.json`、运行配置路径、Q40预留接口均对公网返回403。
- 原9份站点/续期配置及证书文件哈希未变化，其他7个容器的ID、启动时间、重启计数未变；Apache主进程716、旧TAORAN主进程643未变。既有DSM健康检查200，知识库页面200，知识库认证入口302，与切换前一致。

这只是成功样本，不抹除之前出现过的模型证据校验失败；相关失败保护仍保留，不能宣称模型永不超时或永不失败。

## 证书与运维

- 独立站点：`/etc/apache2/sites-available/taoran.yudaozhijian.top.conf`；源配置保存在项目`deploy/taoran-https.conf`。
- 后端仍只监听127.0.0.1:8030；未修改防火墙、安全组、其他站点或其他项目容器。
- 证书保留在标准位置`/etc/letsencrypt/live/taoran.yudaozhijian.top/`；不复制私钥进代码或文档。
- 证书验证目录：`/TAORAN agent/acme/.well-known/acme-challenge/`，HTTP保留此路径用于后续续期，其他HTTP请求跳转HTTPS。
- 原有`certbot.timer`处于active。新证书的续期配置单独保存了webroot与专属部署钩子；钩子只匹配TAORAN证书，先检查Apache配置，再平滑加载。
- 仅针对TAORAN证书的续期演练已通过（`renew --cert-name taoran.yudaozhijian.top --dry-run`），测试证书没有保存或替换正式证书。旧版工具默认随机等待约455秒；仅中断本任务的等待进程，使用其已支持的`--no-random-sleep-on-renew`重跑成功，未调整原定时器策略。演练本身跳过部署钩子，因此另行检查并执行本项目钩子验证平滑加载。
- 日志在项目`logs/`；访问日志不含查询参数、认证头或请求正文。新增长度受控的专属轮转规则`/etc/logrotate.d/taoran-agent`，不修改全局轮转策略。
- 首次证书调用因旧版工具不能正确校验带空格的钩子命令而在申请前失败，改用`/bin/sh`启动项目脚本后成功；未升级服务器软件或禁用校验。

实施采用[Certbot webroot模式](https://eff-certbot.readthedocs.io/en/stable/using.html#webroot)以保留现有Web服务运行，站点配置检查通过后使用[Apache平滑加载](https://httpd.apache.org/docs/2.4/stopping.html#graceful)。已安装版本为Apache2.4.41、Certbot0.40.0，本轮未升级。

## 下一步：仅切换简道云测试副本

1. 在测试副本中将AI按钮和提交后通知改为上述HTTPS地址，不动正式表；保持原认证与签名方式。
2. 启用并核验过程详细描述、联系人和商机等按钮字段绑定，不能仅凭字段ID映射正确就认为已经传入。
3. 使用已授权的专用测试记录完成页面点击、修改后重检、提交后评分及两个AI字段真实回写验收；避免触发其他业务流程。
4. 验收通过后再另行决定正式表迁移。现有本地POC、旧服务器服务不在本轮停用范围内。

## 审计与回退

配置前后审计在服务器`audit/before-https-20260826.json`、`audit/after-https-20260826.json`。公网测试报告在本地项目`outputs/server-deployment-20260826/public-https-*.json`，同步副本放服务器`audit/https-20260826/`。

如需暂停公网，只禁用`taoran.yudaozhijian.top.conf`这个站点并在配置检查后平滑加载，保留独立容器、数据库、证书和备份；不要停止共享Apache、全局重启Docker或删除其他域名记录。已保留TAORAN原HTTP验证配置作为回退依据。
