# 简道云剩余接入核查

日期：2026-08-26；目标仅为`拜访记录录入_AI测评`，应用`60fe7ad79ca2d000075dfab1`、表单`6a8408b7c5a0d9454090a5bc`。不修改正式表、40题或知识库。

## 最新进展：已切换企业内部插件

用户确认充值并授权后，已创建、启用“TAORAN拜访草稿检查”，替换测试副本原单个AI检测动作；21项输入及仅意见输出保存后重新加载核验。平台调试与服务器审计验证多行、两行联系人、两行商机、清空重检及成员编号提取；本轮未提交业务记录，未调整后评Webhook、40题、正式表或其他服务器服务。详情及空日期省略边界见《简道云插件接入验收_20260826》。以下17:38及更早记录保留历史，不代表目前仍使用自定义请求。

## 历史进展：提交前AI按钮入口已迁移（17:38，北京时间）

- 用户要求“调整前端AI检测”后，仅将测试副本现有`TAORAN AI检测`自定义POST动作URL改为`https://taoran.yudaozhijian.top/api/v1/connectors/jiandaoyun/visit/button-check`。动作仍只有1个，返回`$.feedback_text`到“AI反馈意见”，不绑定“AI评分”。
- 保存事件、保存测试副本表单并重新加载，重新打开事件确认新URL持久存在；Header/Body编辑窗口修改前后快照一致，未改动原有认证及参数绑定。本轮未更改提交后Webhook、正式表、40题项目，也未创建插件、修改支付配置或重启服务。
- 在简道云前端事件调试窗口，使用空白调试字段连续发送2次请求（未提交业务记录）。服务器日志分别于`17:36:20`、`17:36:42`记录HTTP 200；两个返回结果均有中文`feedback_text`，检查ID不同，`official_score_generated=false`，反馈包含6项检查标准。
- 独立HTTPS合成接口回归8组通过：完整输入、重复检查、日期早于、日期同日、修正日期、清空过程、漏传过程及错误子表。7组正常返回HTTP 200，错误子表按预期HTTP 422；无鉴权按预期401。正常响应测得约56–147毫秒，该时间不是用户页面点击的完整耗时。
- 仍未闭环：现场字段选择器搜索“过程”无候选；现有自定义请求仍未传`process_description`、`participants`、`opportunities`，也没有已确认的下一次行动对象独立来源。反馈会明确“接口未获取”，不当作用户未填写，不默认达标。提交后已验证能收到过程描述，不可据此替代当前草稿。
- 本次完成入口切换与平台调试连通，不等于完整六项前端字段检查验收。后续补齐多行/子表传递如需内部插件及相关计费配置，须另行明确确认；本轮不新增镜像字段、不将过程原字段改成单行、不以已保存记录代替草稿。
- 下文历史章节中“前端未迁移”已由本节更新；字段漏传限制仍有效。

## 真实提交验收：用户提交后的评分与回写已跑通

- 用户确认已提交真实测评数据后，只读检查独立服务器任务、访问日志及简道云目标记录；本轮未手工触发评分、重试回写或修改拜访内容。
- 记录编号：`BFJL2026082600030`；测试副本数据ID：`6a8eb1fcadd02077cdbedc1d`；任务：`job_71ffc22a40b0b02dbe55`。
- 北京时间`2026-08-26 17:29:32`收到真实提交的Webhook，HTTP 202；任务于`17:29:45`完成，服务器接收到任务完成耗时约`13.507秒`，不是浏览器点击提交开始的总耗时。
- 模型为`glm-5.2`，语义分析状态`completed`，模型耗时`13249毫秒`，未记录失败原因。
- 本次Q33为`18.75/50`，Q34为`0/50`，总分`18.75/100`。分项：完整性18.75/25、24小时及时性0/25、自评一致性0/35、下一行动合理性0/15。这是实际引擎输出，本轮流程验收不等同于评分规则业务准确性复核。
- “过程详细描述”已进入提交后分析请求，长度207字符；不在验收文档保存原始客户内容。这只证明提交后传递，不证明提交前按钮漏传已修复。
- 任务记录回写成功，写入范围仅“AI评分”和“AI反馈意见”。随后通过简道云读取接口重新读取同一记录：评分文本与预期`18.75`完全一致，反馈意见1358字符且与任务生成内容完全一致。
- 已确认真实提交→服务器接收→模型分析→评分→两项AI字段回写→云端读回一致。前端按钮迁移仍未完成，未修改正式表及40题项目。

## 提交后推送地址切换记录（17:28，北京时间）

- 用户决定暂不创建插件，优先由用户在简道云测试副本直接提交真实记录，再追踪提交后评分及回写。
- 经用户明确确认，仅编辑现有“TAORAN提交后深度评价”的服务器地址：`https://taoran.yudaozhijian.top/api/v1/connectors/jiandaoyun/visit/webhook?tenant_id=tenant_demo`。
- 推送名称、测试表单、Secret、备注和触发事件均未操作；事件仍为“有新数据提交时”“有数据被修改时”，其余事件未勾选。未新建推送，未改前端AI按钮、正式表、插件或支付配置。
- 简道云“服务器连接测试”显示“服务器连接成功”；独立服务器访问日志在`2026-08-26 17:27:56 +0800`记录该Webhook的POST请求，返回HTTP 202。
- 简道云显示“保存成功”；重新加载应用后台后，目标行仍显示新地址和原有两个事件。与修改前页面对照，另一条“DSM销售管理智能测评”的推送地址及事件未变。
- 以上仅证明配置保存和服务器连接成功，不代表真实记录已完成评分或回写。下一步待用户提交测试副本中的记录并提供记录编号、提交时间，再核对任务接收、GLM分析、Q33/Q34各50分及两个AI字段实际回写。
- 下列章节保留为本日较早的核查历史；其中“尚未切换/尚未读取提交后通知”的描述已由本节更新。提交前按钮仍未迁移，不能将本次提交后接入视为前端漏传修复。

## 已直接核实

- 已打开正确测试副本的表单设计页，AI检查按钮配置了1个执行动作，名称为`TAORAN AI检测`。
- 该动作仍为自定义POST请求，地址是`https://polite-rivers-stick.loca.lt/api/v1/connectors/jiandaoyun/visit/button-check`，尚未切换到独立服务器。
- Header已有租户与服务认证配置，核查输出隐藏密钥；本轮未改写密钥。
- Body绑定租户及18项已有输入，缺少`process_description`、`participants`、`opportunities`。返回值绑定“AI反馈意见”。
- 本地历史审计识别专用测试记录：`BFJL2026081900001`，数据ID为`6a851bd917936e24c254f9f5`；本轮未提交、更新或发送该记录给模型，正式测试前还需权威数据只读核验。
- 现有本地插件代码为`integrations/jiandaoyun/button_check.js`，支持多行原文转发、两个子表的必要字段筛选、12秒超时及失败中文提示。线上完整子表绑定能力仍未核实，不能宣称漏传已经修复。

## 执行前置与暂停原因

1. [简道云管理自建插件说明](https://hc.jiandaoyun.com/open/23483)目前载明：后端自建插件触发扣除0.0001云币，1万次触发约1元，独享版例外；启用需要云币支付条件。现有企业余额、支付开关未核实，不擅自充值或修改企业级支付设置。
2. 需要在简道云企业内部插件受控配置中保存现有TAORAN服务密钥，以向`taoran.yudaozhijian.top`鉴权；不使用或上传智谱Key、知识库Key、简道云管理API Key。按Chrome浏览器技能的安全规则，保存云端配置、密钥传递及计费操作前须明确确认。
3. 企业管理页可读取，但点击“版本信息”等操作连续出现浏览器控制超时。已按Chrome技能检查当前页面及可见控件，未使用绕过浏览器的脚本、会话凭据或隐藏接口。停止继续重复失败操作，待浏览器恢复后继续。

本轮只完成只读核查，没有新建或启用云端插件，没有保存按钮、Webhook、表单或支付开关变更。提交后通知的最新线上地址尚未重新读取，不以历史文档代替现场确认。

## 恢复后的顺序

1. 确认插件授权及计费边界，核查当前云币条件；遇到新增企业级支付开关、充值或协议，另行交由用户确认。
2. 创建专用内部插件，配置21项请求参数和受控通用参数；先验证多行及两个子表能否正确绑定，不能用旧记录补齐当前草稿。
3. 测试副本AI按钮切换为插件，只把本次意见填入“AI反馈意见”，前端不写AI评分；旧执行动作保留恢复依据但不并行执行。
4. 检查并切换该副本的提交后签名通知到独立HTTPS，不更改正式表。
5. 实际业务数据传输前明确专用测试记录、目的地（独立TAORAN服务器及提交后智谱GLM-5.2）、数据范围和允许改写的两个AI字段；先做合成草稿测试，再做专用记录真实回写验收。
6. 保存最终结果，区别接口测试、云端配置保存和实际页面端到端验收，不混用完成状态。
