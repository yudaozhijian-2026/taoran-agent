# 简道云接入方案

## 1. 新建“AI检测”按钮

2026-08-26现场检查发现：现有自定义请求不支持引用多行文本，原按钮未传“过程详细描述”。已补齐支持多行文本的专用插件代码与本地测试，**尚待确认后在测试副本启用**。绑定清单和线上验收见[过程详细描述传参接入.md](过程详细描述传参接入.md)；不能仅凭后端字段映射成功就判定按钮已传入该字段。

在拜访记录填写页增加“AI检测”按钮，通过前端事件发送当前未提交表单数据：

```text
POST /api/v1/connectors/jiandaoyun/visit/button-check
X-Tenant-Id: <租户ID>
X-API-Key: <TAORAN服务密钥>
```

每次点击生成新的`request_id`，并传入当前表单修订号。返回内容用于展示缺失字段、空泛表达、补全追问和规范性
修改意见。按钮不得绑定“提交时校验”，也不得设置“AI未通过不能提交”的公式或流程条件。

固定前端策略：

- AI检查失败、超时或返回`review`都不阻断保存；
- `can_submit`始终为`true`；
- 用户修改字段后可再次点击检查；
- AI建议单独展示，不自动覆盖用户输入；
- 业务可见反馈不重复显示“不阻断提交”提示文字；
- 前端事件未实际传入的字段不作为“用户未填写”提示，避免连接器漏传造成假缺失；
- 前端同步目标8—12秒，超过预算时展示“已给出基础检查，深度评价将在提交后完成”。

建议为按钮结果增加：AI检查状态、记录完整率、缺失项、规范性修改建议、检查版本、检查时间和Trace ID。

## 2. 正式提交后触发深度评价

拜访记录正式保存后，测试副本通过简道云“数据推送”调用签名Webhook：

```text
POST /api/v1/connectors/jiandaoyun/visit/webhook?tenant_id=tenant_demo&nonce=<简道云生成>&timestamp=<简道云生成>
X-JDY-Signature: <简道云生成的SHA-1签名>
```

当前数据推送名称为`TAORAN提交后深度评价`，监听`data_create`和`data_update`。Agent使用配置的Webhook Secret按以下规则验证签名：

```text
sha1("{nonce}:{raw_payload}:{secret}:{timestamp}")
```

签名通过且应用ID、表单ID均匹配测试副本后，Agent立即入队并返回202，深度评价和回写在后台完成。连接测试等非数据事件返回202并标记为忽略。Webhook Secret只保存在`.env`：

```text
DSM_TAORAN_JIANDAOYUN_WEBHOOK_SECRET=<secret>
```

如由DSM或其他受信系统直接调用，可继续使用内部接口：

```text
POST /api/v1/connectors/jiandaoyun/visit/submitted
```

内部请求包含应用ID、表单ID、数据ID和租户认证；Agent通过简道云V5单条记录接口读取权威数据，再异步评价。底层通用评价接口仍为：

```text
POST /api/v1/connectors/jiandaoyun/visit/evaluations
```

通用请求应包含：

- `form_data`：正式记录的原始widget数据，包含`_id`和`_createTime`；
- `writeback_target`：应用ID、表单ID、数据ID和输出字段白名单映射；
- 唯一`request_id`和当前`form_revision`。

接口返回`202`和`job_id`。简道云无需等待评分完成；可按`job_id`查询，或直接依赖Agent完成API回写。

2026-08-19已完成服务器连接测试、数据推送保存和页面真实修改验收。专用记录页面提交产生的`data_update`签名事件由TAORAN返回202，异步任务完成Q33 100、Q34 0、总分100，并成功回写两个AI字段。随后通过第二次页面提交恢复原始过程描述，恢复任务也成功完成；权威数据读回确认无验收标记残留，AI写回没有形成递归事件。

本次还确认：通过V5数据更新接口修改记录，即使传入`is_start_trigger=true`，也未触发当前数据推送。因此生产流程应以简道云页面提交或经实际验证可触发数据推送的业务入口为准；TAORAN自身回写继续固定使用`is_start_trigger=false`。

## 3. 测试副本AI回写字段

测试副本`拜访记录录入_AI测评`已包含以下两个字段：

| 字段 | 副本实际类型与写入规则 |
|---|---|
| AI评分 | 单行文本；写入字符串形式的TAORAN总分0—100 |
| AI反馈意见 | 多行文本；写入Q33/Q34分项、问题和建议 |

测试副本应用ID为`60fe7ad79ca2d000075dfab1`，表单ID为`6a8408b7c5a0d9454090a5bc`；两个输出字段ID已写入
配置。回写只允许这两个字段，不覆盖销售原始内容；Agent设置`is_start_trigger=false`并使用评价ID作为事务ID。

## 4. 密钥与输入字段自动同步

2026-08-18已完成API鉴权和测试副本字段同步：29个映射项全部匹配，0个未解析，结果已写入活动配置。
API Key只保存在本地环境配置中，不写入项目文档或字段映射文件。

在TAORAN服务的`.env`按租户配置简道云API Key：

```text
DSM_TAORAN_JIANDAOYUN_API_KEYS_JSON={"tenant_demo":"replace-with-api-key"}
```

先预览字段标题与widget ID匹配结果，再确认写回：

```bash
uv run taoran-agent sync-jiandaoyun-fields --tenant-id tenant_demo
uv run taoran-agent sync-jiandaoyun-fields --tenant-id tenant_demo --apply
```

该命令调用简道云`POST /v5/app/entry/widget/list`，支持子表字段；不会读取或修改业务数据。

回写失败不会丢失评分结果。修复配置或网络后调用
`POST /api/v1/visit/evaluations/{job_id}/writeback`重试。

## 5. 上线前验证

- 保持测试副本API Key有效并限制必要权限；
- 副本字段调整后重新执行字段同步，并处理所有关键`unresolved`项；
- 验证按钮事件能发送对象、数组、子表和自定义请求头；
- 验证超时情况下仍可正常提交；
- 验证提交后任务、回写、回写重试和`is_start_trigger=false`；
- 验证相同请求幂等、相同数据更新后的新修订可重新评价；
- 验证两个AI字段不会触发递归评价；
- 完成实际网络下15秒按钮响应和并发压测。

官方资料：

- 前端事件：https://hc.jiandaoyun.com/open/12115
- 对接自有Agent：https://hc.jiandaoyun.com/doc/24952
- AI功能：https://hc.jiandaoyun.com/doc/23882
- V5单条数据接口：https://hc.jiandaoyun.com/open/14221
- 数据推送与签名校验：https://hc.jiandaoyun.com/open/11507

本方案刻意不使用“提交时校验”作为AI门禁。
