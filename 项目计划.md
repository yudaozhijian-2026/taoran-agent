# TAORAN原则评分智能体项目计划

## 已完成：独立运行基线

- 独立FastAPI服务、CLI、Docker构建和健康检查；
- Q33、Q34规则复制并改造成100＋100＝200分的单条记录评分；
- 简道云AI检测按钮接口，提交前固定只建议、不阻断；
- 提交后异步深度评价、任务查询和简道云API回写重试；
- 测试副本47项字段名称、联系人信息和多商机字段适配；
- 租户认证、幂等、输入Hash、规则/Agent版本和SQLite审计；
- 可插拔语义服务及本地降级；
- 单元测试、接口测试和回写测试。
- 默认关闭的Q40兼容检查、周期事实查询、批量补评和独立服务认证预留；

## 下一步：简道云POC

1. 在`拜访记录录入_AI测评`副本新建“AI检测”按钮，不设置提交校验；
2. 配置副本API Key并自动同步输入字段widget ID；
3. 检查自动同步报告中的未匹配字段；
4. 配置TAORAN访问密钥和简道云租户API Key；
5. 连接按钮事件与提交后事件，验证子表、数组和系统时间；
6. 验证评价、API回写、失败重试和防循环触发；
7. 完成15秒内响应、并发、超时降级和幂等测试；
8. 用业务专家确认的低/中/高质量样例校准Q34语义判断。

## 产品化阶段

- 将SQLite替换为PostgreSQL，引入共享幂等和可恢复任务队列；
- 接入正式语义模型、最小化上下文检索和模型可观测性；
- 建立Q33/Q34周期聚合服务，与单条投影分隔离；
- 增加经理处理状态、训练任务和日报联动；
- 完成租户隔离、限流、成本、监控、告警和审计产品化。

## 发布门禁

规则Schema校验、代码回归、黄金样例双人复核、版本签名、生效时间、历史版本只读、租户隔离、超时降级、
回写白名单、防递归和“不编造客户事实”测试全部通过后，才可切换生产配置。
