# TAORAN原则评分智能体架构

## 目标架构

```text
简道云拜访记录表
├─ 点击“AI检测” ──同步──> TAORAN /button-check
│                            ├─ 字段标准化
│                            ├─ 加载已审核TAORAN知识快照
│                            ├─ T—A—O/KR—R—A—N六项规则检查
│                            ├─ 本地空泛表达检查（无外部模型调用）
│                            └─ 返回规范性修改意见（不阻断）
│
└─ 正式提交 ──────异步──> TAORAN /evaluations
                             ├─ 固化首次提交时间和输入快照
                             ├─ Q33确定性评分（100）
                             ├─ GLM-5.2结构化事实＋Q34确定性评分（100）
                             ├─ 汇总总分（100）
                             ├─ 形成AI评价与辅导建议
                             └─ 简道云API白名单回写
```

评分逻辑独立于简道云。简道云只承担数据采集、按钮交互、事件触发和结果展示，标准API也可供其他DSM/CRM调用。

## 提交前：快速建议链路

“AI检测”是普通按钮，不是表单提交校验：

- `can_submit=true`、`submission_blocked=false`；
- 缺字段、内容空泛都只形成提示；前端不调用远程语义服务；
- 不自动改写销售填写的事实；
- 每次点击生成新的`request_id`，相同请求只允许对应一个输入快照；
- 目标8—12秒内返回，低于简道云前端请求15秒上限。

同步链路只读取当前表单数据和本地已审核知识快照，不访问大模型或远程知识API，也不拉取大范围历史数据。规则检查目标低于0.5秒；模型未配置、调用超时或模型槽位已满均不会阻塞前检。网络、适配和界面回填仍保留余量。

## 提交后：深度评分与回写

正式提交后由简道云事件或DSM消息发起后台任务。接口立即返回`job_id`，评分和回写不占用前端响应时间。

评分采用“AI提取受控事实、代码计算最终分”的方式：

- Q33完全由结构化字段和系统时间计算；
- Q34的KR质量、过程事实、达成判断、客户共识和下一行动逻辑由语义模块输出结构化事实；
- 最终35/15分值、客户分层门槛和100分汇总由版本化代码计算；
- 外部模型不能直接写最终分，也不能制造客户事实；
- 回写失败不撤销评价，可通过重试接口恢复。
- 智谱`glm-5.2`按实际JSON Schema生成六项分析和候选事实；纯格式失败最多重生成一次，共享45秒总预算。证据、额外字段、网络或权限失败不自动重试；失败则暂停两个AI字段回写。

## 模块边界

| 模块 | 职责 |
|---|---|
| API/Gateway | 租户认证、幂等、任务状态、Trace和接口版本 |
| Connector | 简道云widget ID和值域到标准字段的转换 |
| Knowledge | DSM知识API离线同步、有效状态筛选、快照Hash和知识来源追溯 |
| Precheck Engine | 按TAORAN六项知识规则执行提交前规范性检查 |
| Scoring | Q33/Q34确定性规则和100分汇总 |
| Semantic Reviewer | 受控语义事实判断；支持HTTP模型和本地降级 |
| Audit Store | 输入Hash、规则/Agent版本、请求和评价结果 |
| Writeback | 仅按输出字段白名单调用简道云更新API |

首版在同一FastAPI服务中运行，便于交付和控制延迟。生产多实例部署时，将SQLite与进程内锁替换为PostgreSQL、共享
幂等存储和可恢复任务队列；只有出现独立扩缩容或多Agent编排需求时再拆服务。

## 治理要求

- 生产环境按租户配置独立API密钥；
- 使用不可变首次提交时间，不以最后更新时间代替；
- 原始销售记录只读，AI结果写入独立字段；
- 简道云回写设置`is_start_trigger=false`，避免循环触发；
- 保存输入快照Hash、规则版本、Agent版本、Trace和评分明细；
- 单条投影分与40题周期汇总分明确隔离。

## Q40预留边界

Q40集成是TAORAN内部的可选入站接口，默认关闭。TAORAN不连接、不写入、不迁移40题现有数据库，也不主动触发40题评分；
将来只有40题先检查规则兼容并主动调用时，才读取TAORAN已经保存的逐条事实或发起历史补评。两套产品可独立发布和回滚。
