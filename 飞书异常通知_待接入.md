# TAORAN 飞书异常通知（本地候选，未部署）

## 通知范围

- 前端自动重试结束后的 AI 最终反馈生成失败或正文为空。
- 实时分析失败但最终意见成功，独立说明实时分析异常。
- 点击“已读并返回”后弹窗检测到交付失败，使用该任务的访问凭证向后台报告。直接关闭弹窗不报告失败。
- 后台分析失败、正式意见缺失、评分缺失（0 分是正常值）。
- 请求有回写目标，但意见/评分回写未成功，包括既有回写流程的读回确认不一致。

不重新调用模型、不进行语义复核，不改变分析、评分和回写结论。没有回写目标的请求不报回写失败；等待任务和模型内置重试过程不当作失败。

## 运行方式

独立线程每 5 秒读取已持久化的任务终态，写入同一数据库的告警发送队列，再调用飞书机器人。按租户路由，未配置的租户不发送到其他群。同一任务同一次失败去重；服务重启保留发送状态。首次启用每个租户时，仅建立历史基线，不补发旧终态记录；当时仍在运行的任务失败后会通知。

每条通知最多尝试 3 次，失败保留在 `feishu_alert_outbox`，记录安全错误类型；健康接口的 `monitoring.feishu_alerts` 提供 pending/sent/failed 数量。发送失败不阻塞业务。Webhook 超时但飞书已收到时，重试仍可能重复投递，群消息中的告警编号可识别同一事件。

通知仅发送租户、拜访记录编号、任务编号、固定的异常分类、处理建议和告警编号，不发送客户原文、AI 全文、凭证或原始错误响应。

## 待提供的配置

需要目标群的自定义机器人 Webhook；启用签名时同时提供签名密钥。写入部署环境的受保护运行配置，不提交真实值到 Git：

```dotenv
DSM_TAORAN_FEISHU_ALERTS_ENABLED=true
DSM_TAORAN_FEISHU_ALERT_ROUTES={"tenant_demo":{"webhook_url":"https://open.feishu.cn/open-apis/bot/v2/hook/REPLACE","signing_secret":"REPLACE"}}
DSM_TAORAN_FEISHU_ALERT_POLL_SECONDS=5
```

没有签名校验时省略 signing_secret。支持飞书业务响应码检查及签名；请求不跟随重定向。
参考：[飞书自定义机器人官方文档](https://open.feishu.cn/document/client-docs/bot-v3/add-custom-bot)。

应用默认关闭，服务器未修改、未部署。2026-09-08 用户已提供专用异常通知群机器人，已通过实际通知发送模块发送一条明确标记的接入测试，飞书响应成功，发送次数 1；未发送模拟业务失败告警。

待发布配置保存在本机仓库外 `/Users/ydzj/.config/taoran/pending-release/feishu-alerts-20260908.env`，权限 0600，绑定 `tenant_demo`；真实 Webhook 未写入 Git。当前机器人无需提供签名密钥即可通过接入测试。正式告警仍需在后续获准统一部署时安装该配置并启用，不能把通道测试成功视为线上告警已经运行。

## 可观测边界

“已交给简道云处理”不等于表单已经保存。浏览器断网时，客户端失败上报也可能无法送达；插件字段赋值及用户最终保存目前没有服务端回执，不能据此承诺识别所有未保存情形。后台持久化回写由既有读回核验确认。本模块不代替服务器宕机时由外部系统执行的可用性监控。
