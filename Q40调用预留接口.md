# Q40调用TAORAN预留接口

## 隔离原则

这些接口仅为未来接入预留，不修改销售智能测评40题项目，也不主动向40题推送数据：

- 默认`DSM_TAORAN_ENABLE_Q40_INTEGRATION=false`；
- 默认关闭时接口返回404，现有TAORAN按钮、单条评价和简道云回写照常运行；
- 只有TAORAN显式启用且40题主动发起请求时才执行；
- 使用独立`X-Service-Id`和`X-Service-Key`，不复用简道云API Key；
- 批量补评禁止配置简道云回写目标；
- TAORAN只提供逐条事实和单条投影分，40题仍按自己的周期规则计算正式Q33/Q34。

## 将来启用

```text
DSM_TAORAN_ENABLE_Q40_INTEGRATION=true
DSM_TAORAN_Q40_SERVICE_ID=dsm-q40-agent
DSM_TAORAN_Q40_SERVICE_KEYS_JSON={"tenant_demo":"独立的服务密钥"}
```

所有请求携带：

```text
X-Service-Id: dsm-q40-agent
X-Service-Key: <独立服务密钥>
```

## 1. 兼容性检查

```text
GET /api/v1/integrations/q40/compatibility
    ?tenant_id=tenant_demo
    &required_rule_version=TAORAN-Q33-Q34-200-V1
```

40题在正式取数前检查`compatible=true`；不兼容时停止调用TAORAN，不影响40题原有评分路径。

## 2. 周期事实查询

```text
GET /api/v1/integrations/q40/period-facts
    ?tenant_id=tenant_demo
    &employee_id=EMP001
    &period_start=2026-08-01
    &period_end=2026-08-31
    &required_rule_version=TAORAN-Q33-Q34-200-V1
    &expected_visit_record_count=20
```

返回逐条结构化事实，包括Q33字段分子分母、及时性、Q34语义事实、自评一致性、下一行动合格性、是否进入正式Q34
面对面统计范围、版本和输入Hash。`expected_visit_record_count`由40题数据源提供；未提供时覆盖率只声明为TAORAN库内记录数，
不声称已覆盖全部拜访。

## 3. 历史记录批量补评

```text
POST /api/v1/integrations/q40/evaluations:batch
```

请求样例见`examples/q40_batch_evaluation_request.json`。每批最多100条，异步返回`batch_job_id`，相同批次请求ID和
相同输入只执行一次。批次内每条记录仍使用自己的`request_id`完成幂等。

查询批次：

```text
GET /api/v1/integrations/q40/batches/{batch_job_id}?tenant_id=tenant_demo
```

## 存储边界

当前仍使用TAORAN自己的SQLite审计库，不读取或修改40题数据库。为未来多实例共享存储保留
`DSM_TAORAN_SHARED_STORE_URL`配置，但当前必须留空；真正切换PostgreSQL前需单独实施迁移和压测。周期索引字段仅在
TAORAN评价表内新增，历史记录只在Q40接口被主动调用时按需补索引。

## 版本契约

- 接口契约：`TAORAN-Q40-INTEGRATION-V1`；
- 当前规则：`TAORAN-Q33-Q34-200-V1`；
- 最低Agent版本：`0.3.0`；
- 当前仅声明同一规则版本精确兼容，不对未来版本静默兼容。
