# 手动检测与提交后分析流程

状态：本地候选，尚未部署服务或更新简道云在线插件。

## 销售操作

1. 新建拜访记录，主动点击AI检测才出现弹窗；分析当前填写内容。
2. 点击“已读并返回”将意见填到填写页面。直接关闭不填回，也不保存记录。
3. 首次提交后后台自动分析，回写正式意见及评分，不弹窗。
4. 已提交记录修改保存时不自动分析。再次主动点击AI检测，读取服务器最新保存的数据；未保存修改不参与本次正式分析，不弹保存提示。
5. 与最近任务的TAORAN输入一致时，复用正在处理的任务或成功回写的结果；有变化或此前失败时重新分析并自动回写。

## 实现边界

- 插件通过提交后才生成的拜访记录编码区分流程；新建记录编码为空仍正常检测，不需临时标识。
- 编码只在授权租户的绑定表单查找。客户端传入的记录ID、未保存快照不能替换查询出的正式记录。
- 比较字段使用现有映射适配后的分析输入，包含过程、关键结果、下一步、联系人/商机证据、不可变首次提交时间等评分输入；排除AI输出、路由编号和传输元数据。
- 只接受data_create自动分析；data_update通知（含AI字段更新）直接忽略。
- 失败/模型不完整/回写失败的任务不作为成功复用。相关内容变回早期值时，也按最近任务比较，不直接复用过时任务ID。
- 用户要求取消的回写前来源比较不恢复；保留授权、目标表单、评分契约和写后确认。最后完成回写的结果生效。
- 已提交记录的正式回写不依赖弹窗是否关闭；“关闭不回填”指插件不向当前编辑页面返回内容，不是取消已主动发起的后台正式分析。
- Q33、Q34各50分，规则和模型评分契约不变。

## 本地验收与发布边界

391项Python测试通过；48项浏览器逻辑测试通过；Ruff通过。
测试包括重复任务复用、失败可重试、相关内容比较、服务器保存值优先、更新通知不触发、关闭后不回填。
尚未进行本轮线上发布和简道云真实数据验收；需按已约定的独立版本、备份、队列检查及单容器部署流程同步发布服务和插件，不能仅更新其中一侧。
