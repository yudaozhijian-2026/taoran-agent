# TAORAN服务器独立部署

更新时间：2026-08-26。用户已通过截图确认服务器根目录`/TAORAN agent`为本项目目标；部署授权来自本次内部测试与服务器部署请求。

## 隔离边界

- 项目路径：`/TAORAN agent`；源码：`releases/0.7.0-20260826/`；`current`指向该版本。根目录保留`AGENT.md`及项目Markdown文档，运维以本文为准。
- Docker项目：`dsm-taoran-v2`；容器：`dsm-taoran-v2-agent`，不复用旧`taoran-agent.service`。
- 公网入口：`https://taoran.yudaozhijian.top`；后端仍仅监听`http://127.0.0.1:8030`，不开放8030公网端口。独立HTTPS已开通，简道云地址尚未切换。
- 独立数据：`/TAORAN agent/data/taoran_agent.db`；不复制本地历史库、不连接40题或知识库数据库。
- 运行配置：`runtime/agent.env`，权限600，运行用户UID/GID10001，只读挂载；原API Key经SSH传输，不进入镜像、文档或命令参数。
- 正式表、旧TAORAN、40题、知识库及防火墙未修改。经用户确认，仅新增TAORAN DNS记录、Apache独立站点、独立证书及专属日志轮转配置；原站点配置与证书保持不变。Q40集成保持关闭，公网代理额外拒绝其接口。

## 服务约束

使用固定Python3.12基础镜像及uv.lock；单实例非root运行，只读根文件系统，移除Linux能力，禁止权限提升。CPU0.5核、内存768MB、进程128个；日志10MB×3轮转；健康检查与自动重启；优雅退出等待90秒。访问日志关闭，避免签名参数进入访问日志。

服务器内核不支持Docker交换空间限额，因此768MB物理内存限制已核实生效，但不能宣称交换空间限额也已生效。验收时容器空闲内存约51MB，磁盘余量约7.5GB；应持续关注磁盘和内存，不进行全局清理。

提交前仅本地规则给建议；提交后glm-5.2深评，Q33/Q34各50分。模型/证据失败暂停正式回写。只写测试副本AI评分与AI反馈意见。

当前SQLite＋进程内后台任务仅用于受控试运行，不是高可用队列。重启前先确认没有queued任务；异常退出后的未完成任务需要人工检查，不能自动批量重评或覆盖历史评分。

## 维护入口

以下命令在服务器执行，只针对本项目：

```bash
docker compose -p dsm-taoran-v2 -f '/TAORAN agent/deploy/compose.server.yaml' ps
curl -fsS http://127.0.0.1:8030/health
docker logs --tail 30 dsm-taoran-v2-agent
```

不要使用全局容器重启、全局镜像清理、替换Apache配置或旧服务重启。停止新版以回退时，仅停止`dsm-taoran-v2-agent`，保留数据及运行配置；当前简道云尚指向原本地POC入口，不受新版停止影响。

## 公网入口与待完成的简道云接入

用户于2026-08-26确认使用独立域名`taoran.yudaozhijian.top`。当日16:41从服务器直接查询权威DNS（`dns29.hichina.com`），A及AAAA均返回NXDOMAIN，尚无该名称的解析记录；本地网络返回的198.18.*地址是代理环境结果，不能作为公网解析证据。

用户完成阿里云登录并明确确认后，16:56新增：主机记录`taoran`、类型`A`、记录值`47.119.191.148`、默认线路、TTL 10分钟。两台权威DNS及两个递归DNS均返回正确地址。未修改原10条记录，未新增AAAA记录。

独立证书已签发，只包含`taoran.yudaozhijian.top`，有效至2026-11-24。配置检查通过后平滑加载独立站点，外网HTTPS健康检查返回200，HTTP自动301跳转HTTPS。没有替换现有DSM或知识库证书，也没有重启其他项目。详细地址与验收边界见[HTTPS接入验收_20260826.md](HTTPS接入验收_20260826.md)。

公网验收后才切换简道云测试副本按钮/Webhook。原按钮过程、联系人及商机字段云端绑定仍待启用，服务器部署不会自动修复；需现场验证插件字段绑定和费用/权限提示。真实业务回写验收应仅使用已授权专用测试记录。

## 本次部署验收

2026-08-26，新版0.7.0已在确认目录独立运行，内部测试如下。没有修改业务判断规则或放宽模型证据校验。

| 验收项 | 实测结果与边界 |
| --- | --- |
| 自动化回归 | 本地276项通过；代码检查通过。测试工具有一条兼容性弃用提示，不影响本轮结果。 |
| 服务器AI按钮接口 | 8种输入通过，包括重复检测、日期早于/同日、修改后重检、过程清空/漏传、错误子表格式；无授权请求401。成功响应不生成正式评分，展示六项标准。日期异常正常返回修改建议。 |
| 服务器响应测量 | 首轮按钮测试约4.62–196.67毫秒（含422输入错误）；这是服务器内部测量，不代表简道云页面或公网延迟。 |
| 真实模型异步接口 | 约101毫秒接受任务，约11.69秒完成GLM-5.2分析；Q33=50、Q34=50、总分100。没有配置回写目标，因此未写真实简道云。 |
| 签名通知完整链路 | 合成数据、独立临时数据库、真实GLM、模拟简道云写入。首次返回`ungrounded_evidence`，模型证据未通过校验，未通过链路验收。保留该失败，不计为成功。 |
| 完整链路一次受控复测 | 约14.71秒通过；模型一次生成有效结果。仅模拟写入两个AI字段；重复通知不重复写入，错误签名401。实际简道云写入次数为0。 |
| 字段结构只读核验 | 29项匹配、0项未解析，没有更新表单或记录。字段ID正确不等于按钮字段已经绑定。 |
| 重启及持久化 | 确认没有排队任务后，仅重启新版容器；健康接口恢复，原1条已完成评分与7条预检数据保留。重启后8种按钮输入再次通过，内部耗时约3.36–63.92毫秒。 |
| 其他项目隔离 | 40题、知识库7个容器ID、启动时间及重启计数与部署前一致；旧TAORAN主进程643、Apache主进程716未变；40题健康接口正常。 |

结论：后端及独立HTTPS可以运行，快速规则检查及提交后评分链路均已有成功样本；**不能据此宣称模型稳定率100%或简道云页面端到端上线完成**。模型输出仍可能因证据不充分被拒绝，失败时保留原AI字段，需要复核后按受控方式重试。简道云真实页面及专用测试记录的真实回写尚待验收。

服务器测试报告保存在`data/internal-server-*.json`，不含密钥。首轮完整链路因旧测试脚本直接中断，仅有终端错误及本文记录，没有完整JSON；已完善测试脚本以保留后续失败审计信息，不改变业务引擎。

## 构建与可追溯记录

服务器在线下载依赖过慢，本次改用本地准备离线依赖包：23个运行依赖按`uv.lock`选择Linux AMD64/Python3.12版本，逐个核验SHA256，另加本项目0.7.0安装包；服务器构建阶段关闭网络。未升级依赖。

- 镜像：`dsm-taoran-v2:0.7.0-20260826`，约221MB。
- 镜像ID：`sha256:4f984f19eda12593f2fadb94ae9d3c171a05be592d088b7f228bc0a03481162b`。
- 固定基础镜像：`ghcr.io/astral-sh/uv@sha256:e5b65587bce7de595f299855d7385fe7fca39b8a74baa261ba1b7147afa78e58`。
- `audit/taoran-0.7.0-source.tar.gz` SHA256：`1e5b8c9b3e868b3597bb85ab01a74875ac6b3b68796748cc1515eb3ca3d9b5fc`。
- `audit/taoran-offline.tar.gz` SHA256：`c3489eda60d7f99b585b2d34928929cbcc34c9ef11cecf72d1dd233d189b76e3`。
- 离线构建材料：`releases/0.7.0-20260826/offline/`；维护文件：根目录`deploy/`。源代码归档保留不变，后续更新的部署文档、离线准备工具及测试审计工具放在项目根目录，不冒充原始归档内容。

本次只停止了本任务的临时在线构建容器，未删除用户文件、历史数据或其他容器。构建缓存及该已停止容器未做全局清理。
