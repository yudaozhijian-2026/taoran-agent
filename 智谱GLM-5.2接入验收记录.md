# 智谱GLM-5.2接入验收记录

> 2026-08-26评分口径更新：当前Q33/Q34各50分、合计100分，程序0.7.0，见《百分制评分调整与验收》。下文历次验收中的200分、旧版本及测试数量为历史记录，不代表新版结果；云端字段绑定待启用状态未变。

日期：2026-08-26。当前程序版本：0.6.1。当前提示词版本：TAORAN-LLM-FACTS-V2.1。

## 0.6.1展示调整

根据用户截图要求，新生成的AI反馈不再显示“知识依据”和“分析方式”两行。仅改变展示，不改变六项判断、修改建议、Q33/Q34规则、模型调用或后台来源审计。113项自动化测试和构建通过；服务已重启。本次仅用虚构数据检查新反馈格式，未调用大模型、未回写简道云业务记录，也未批量修改历史意见。

## 最新结论（0.6.0）

用户已确认“提交前快速规则检查、提交后GLM-5.2深度分析”。本次实现与小样本技术联通验收通过，实际`.env`已设为`DSM_TAORAN_LLM_ENABLED=true`，本地0.6.0服务已启动，原HTTPS入口`https://polite-rivers-stick.loca.lt`已恢复，简道云地址没有变更。

前端固定本地规则，零模型调用、无正式评分；后端使用专用Key调用`glm-5.2`，通过校验后仍按Q33/Q34各100分计算。本次没有读写任何简道云业务记录；模型接入后的实际页面提交与双字段回写仍需使用专用测试记录验收，不能把模拟回写测试或本次不回写的接口测试视作真实回写成功。

## 本次格式问题定位与修正

V2真实样例中，完整记录用时13.133秒、部分达成记录13.877秒，均一次通过；缺失记录两次尝试共31.201秒均失败。安全诊断明确定位为`sections.*.evidence.*.quote`的`string_too_short`：模型把空字段写成空字符串证据。这是本次复现的问题，不能倒推为V1那次未捕获失败响应的唯一原因。

V2.1在实际Schema基础上补充本次非空可引用字段清单、空字段缺失输出示例和重生成纠错说明：空字段仍列为缺失项，但不得产生空证据。未放宽引用最小长度、字段权限、原文逐字匹配、正式计分或失败暂停回写规则。

### V2.1真实模型测试

| 场景 | 模型耗时 | 结果 | 模型尝试次数 |
|---|---|---|---|
| 缺失记录专项复测 | 12.824秒 | completed，结构与原文证据校验通过 | 1 |
| 完整记录复测 | 13.517秒 | completed，结构与原文证据校验通过 | 1 |
| 部分达成记录复测 | 11.283秒 | completed，结构与原文证据校验通过 | 1 |
| 缺失记录再次复测 | 10.135秒 | completed，结构与原文证据校验通过 | 1 |
| 启动后的真实本地API异步任务 | 12.258秒 | completed，Q33=100、Q34=100、总分200；writeback=skipped | 1 |

上述5次均发送虚构拜访数据和已审核知识快照；只是技术验收小样本，不代表生产稳定性、业务准确率或P95已达标。缺失样例前检返回`needs_revision`为正常规范建议，不是接口故障。

### 服务与接口验证

- 本地和公网`/health`均返回200、版本0.6.0。
- 本地按钮接口相同数据连续调用3次均返回200，客户端耗时约1.32—3.91毫秒，检查ID各不相同；不返回任何正式分数。
- 公网按钮虚构缺失记录返回200，客户端耗时735.96毫秒，`official_score_generated=false`、`can_submit=true`，反馈注明“本地快速规则检查”。这是接口调用用时，不是简道云页面全链路渲染用时。
- 提交后标准接口约1.57毫秒返回202，后台任务`job_0b1f27d19fee212be846`完成GLM-5.2分析、规则计分和本地审计持久化；因未指定回写目标，明确跳过简道云回写。
- 109项自动化测试、静态检查、安装包构建通过。覆盖前端在模型槽位满时仍快速返回、格式失败最多重生成一次且共用45秒、失败响应不泄密、证据与额外分数字段不自动修复、失败不覆盖AI字段。
- 本地服务进程PID为7900（临时运行标识，重启会变）；本地服务与LocalTunnel必须保持运行。当前仍为本地POC，不是具备常驻守护与固定公网域名的生产部署。

## 初始验收（0.5.0 / V1，历史记录）

以下为本轮调整前的状态，保留用于追溯，不代表当前启用状态。

TAORAN专用Key已在本地配置，用户指定模型为`glm-5.2`。已使用虚构拜访样例完成真实API请求，证明Key和模型访问权限有效；但两阶段整体验收未通过，不能宣称已稳定启用。

实际`.env`保持`DSM_TAORAN_LLM_ENABLED=false`，提交前6秒、提交后45秒的原有预算未改动。本次未启动服务，检查时本地8030端口没有监听进程。

### V1真实调用结果

| 轮次 | 阶段 | 本次临时模型预算 | 用时 | 结果 |
|---|---|---|---|---|
| 1 | 提交前 | 6秒 | 6.006秒 | 超时，未获得可用模型分析 |
| 1 | 提交后 | 45秒 | 15.553秒 | 完成，六项结构与原文证据校验通过 |
| 2 | 提交前 | 10秒 | 10.005秒 | 超时，未获得可用模型分析 |
| 2 | 提交后 | 45秒 | 18.542秒 | 返回格式不符合约定（invalid_contract），拒绝作为正式结果 |
| 3 | 仅提交后诊断 | 45秒 | 16.384秒 | 完成，六项结构与原文证据校验通过 |

第二轮10秒预算只用于独立测试进程，没有写入`.env`，没有延长简道云前端等待时间。第三轮未复现格式问题，尚不能确定第二轮具体是哪个字段不合规；没有放宽校验或把失败响应转换成通过。

这些数据是5次真实模型调用的小样本结果，不代表业务准确率、稳定性或P95延迟达标。模型失败时的保护逻辑有效，不等于模型业务验收通过。

## 安全和影响范围

- Key仅从TAORAN本地`.env`读取，未输出到聊天、日志、文档或URL；文件权限为仅当前用户可读写，且被Git忽略。
- 只发送虚构拜访样例与已审核TAORAN知识快照；未读取或回写简道云业务记录。
- 未读取40题密钥，未修改40题代码、配置或评分流程；Q40预留集成保持关闭。
- 初始版本90项测试通过，当前0.6.0为109项测试通过；自动化测试不替代真实调用和业务验收。

## 下一步验收

1. 用户在简道云测试副本的专用记录点击AI检测：只显示规则未达标项与中文建议，不更新AI评分，可重复检测。
2. 正式提交专用测试记录：检查202入队、GLM-5.2审计、Q33/Q34总分与两个AI字段回写；确认原始销售字段不变且无循环触发。
3. 增加专家标注的低、中、高质量样例，核查实际达成、客户共识与下一行动逻辑的准确性，再做负载/故障测试。
4. 正式迁移前补齐固定HTTPS入口、常驻进程守护和可恢复任务队列；不自动改动40题或正式拜访表。

模型与接口依据：[智谱GLM-5.2官方文档](https://docs.bigmodel.cn/cn/guide/models/text/glm-5.2)。
