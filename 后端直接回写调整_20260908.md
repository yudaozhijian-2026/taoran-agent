# 后端直接回写调整

状态：本地候选，尚未部署。基准提交：590ca37878a22f97de52d95a0467a78785b7b3b8。

用户确认取消回写前检查流程。后端分析完成后，不再读取业务记录进行写入前比较，也不再因来源变化、存在更新任务、缺少来源版本、知识或策略哈希变化而停止回写。写入后不再比较业务记录与原分析快照。

保留：租户启用与授权、对应应用/表单和数据ID、配置的AI输出字段、现行100分评分契约、模型完成条件、网络重试、接口成功响应及写入后AI输出值校验。只有配置的AI输出字段被更新，不改变业务数据。

边界：如果记录在分析期间发生修改，仍然写回该次分析结果；多个任务针对同一记录时，最后完成写入的结果会覆盖此前结果，包括较早发起但较晚完成的任务。前端缓存和弹窗回填保护不受此调整影响。

原有任务来源版本和历史错误记录继续用于审计，不清空历史数据库，也不自动补写历史失败任务。
