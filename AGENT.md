# DSM TAORAN原则评分智能体

## 身份与目标

本Agent是独立运行的DSM拜访记录检查和评分能力，代码为`DSM-TAORAN-SCORING-AGENT`。
它整合销售管理智能评测40题中的Q33、Q34规则：Q33满分50分、Q34满分50分，综合满分100分。

简道云是首个交互和回写对象，但评分逻辑不依附简道云，其他DSM或CRM系统也可通过标准API调用。

## 权威来源

按以下优先级执行：

1. DSM知识服务中已批准/已确认的第六层正式知识，当前为`DSM-BS-000`和`DSM-BS-01-07`；
2. DSM统一行为数据模型V1.5；
3. Q40 V4正式标准中的Q33和Q34；
4. Q40字段映射中的Q33-v2、Q34-v5执行契约；
5. 租户生效的客户类型、拜访目的和商机阶段策略；
6. AI输出的结构化语义候选事实。

AI不能覆盖来源事实，不能直接计算最终分，也不能阻断简道云提交。

## 两阶段工作流

### 提交前：AI检测按钮

```text
销售填写拜访记录
→ 点击“AI检测”
→ 字段适配与输入快照Hash
→ Q33完整性预检
→ 本地空泛表达与TAORAN规则检查（不调用大模型）
→ 返回缺失项、空泛表达、追问和规范性修改建议
→ 简道云展示结果
→ 销售自行决定修改或直接提交
```

提交前策略固定为`advisory_only`：

- `can_submit`始终为`true`；
- `submission_blocked`始终为`false`；
- 不配置提交校验阻断；
- AI意见不得自动改写销售填写的客户事实；
- 面向销售和经理的AI反馈必须显示简道云实际中文字段名，不得暴露英文内部字段路径；
- 提交前和提交后的AI反馈不显示“知识依据”和“分析方式”说明行；来源、版本及模型信息保留在结构化结果和后台审计，不改变规则、具体分析或异常提示；
- 同一份表单允许重复点击AI检测；每次点击生成独立检查请求，修改内容后可立即重新检查；
- 下一次联系客户时间安排早于或与本次拜访日期相同，不在输入解析时抛出异常；正常返回“下一次联系客户时间安排：异常”，列出两个日期、异常原因及按实际计划修正的建议。未填写单独提示，不混同为日期异常；
- 日期比较按简道云业务时区（Asia/Shanghai）的自然日执行，原始时间保留供审计。未收到拜访日期或临时使用默认日期时，不凭默认日期推断下一次联系时间异常；
- 上述异常处理不放宽提交后日期门槛：日期未晚于本次拜访时，Q34下一行动仍不合格，大模型不能覆盖此判断；
- 目标8—12秒返回，不占满简道云15秒请求上限。

AI反馈意见固定使用知识库确认的TAORAN六项结构：

- `T｜客户类型`：客户类型、客户状态、商机阶段与拜访目的是否匹配；
- `A｜预约与拜访方式`：是否预约，拜访方式是否支持本次目的；
- `O/KR｜拜访目的与关键结果`：目的是否明确，关键结果是否具体、可验证；
- `R｜过程事实与结果`：是否记录客户角色、确认事项、事实、条件、异议或承诺，并区分事实、判断和假设；
- `A｜达成评价`：Assessment是否回到关键结果并由过程事实支持；
- `N｜下一步客户行动`：是否包含时间、对象、目的、期望结果，并承接本次客户事实。

提交前默认检查由`TAORAN-PRECHECK-KB-V2`执行。运行时只加载本地已审核知识快照，不同步等待远程知识API；知识同步失败不影响默认规则反馈。六项判断同时记录联系人角色、客户事实、客户承诺、异议/条件、销售判断、假设和计划行动等结构化证据，并保存标准、知识快照、字段映射及模型提示词版本审计；这些证据增强不改变Q33/Q34的50+50评分结构。

0.11.4将简道云“AI检测”改为一次返回三份反馈：原本地规则反馈不变；纯大模型反馈不注入知识库；知识库反馈每次实时调用DSM知识库API，将本次取得的内容交给智谱模型。知识API失败时不得回退到本地快照冒充实时结论。三份反馈均为`advisory_only`，不生成提交前正式分数。提交后重新生成知识库与纯大模型填写反馈并写入各自字段；即使深度评分证据校验失败，这两个非评分字段也应独立回写。历史任务重试回写时若缺少这两项，也要先重新生成再写入。

0.6.4按用户最新要求，提交前标题改为“TAORAN六项检查：”，六项及各自标准始终全部显示，替代此前仅列未达标项的展示要求。达标项显示“达标＋检查标准”；未达标项显示“未达标＋检查标准＋数据分析＋修改建议”。状态取自规则引擎真实检查结果，不能因没有问题就推断达标：未获取相关字段标“未检查”，仅收到部分字段且已检查内容无问题标“待复核”；两者也显示标准，但不误报用户未填写。检查标准为本地已审核知识与现行前检规则的中文摘要，字段使用简道云实际中文名称；仅作展示，不新增判断条件或评分要求。下一步客户行动标准明确按北京时间自然日判断日期晚于关系，与日期异常说明保持一致；提交后反馈、评分公式及40题项目保持原样。

### 本地前检＋大模型后评（0.6.0）

- 通过独立`DSM_TAORAN_LLM_*`配置启用直接Chat Completions模型；不读取、修改40题模型配置或自动复用其Key。
- 模型分析六项TAORAN，输出结构化结论、中文理由、建议和逐字原文证据，不输出业务分数或工具调用。
- 模型只接收必要拜访内容与已审核知识快照；不发送租户、员工、客户、商机稳定ID、完整元数据或附件。联系人对象和商机编号用匿名编号替代；自由文本中的业务信息仍会传给授权模型提供方。
- 未收到的字段不能被模型当成用户未填写；模型的引用字段必须来自本次输入，原文证据必须能逐字匹配。
- 提交后有证据的模型语义判断用于KR、事实性等候选事实；字段缺失、日期、预约等结构规则及Q33/Q34计分公式不由模型覆盖。
- 提交前规则分支固定本地快速检查，不访问大模型或远程知识API；纯模型和实时知识库分支并行执行，仍不评分。
- 提交后使用用户指定的智谱`glm-5.2`，最多等待45秒（含一次格式重生成），输出Q34候选事实及六项具体分析，由规则计算100分；模型或证据校验失败时保留参考结果供审计，但暂停两个AI字段的正式回写。
- 输出契约由实际校验模型的JSON Schema生成，提示词版本为`TAORAN-LLM-FACTS-V2.1`；空字段不生成空证据；不自动把字符串布尔值、null、错误枚举等强制转换为合格事实。
- 仅JSON语法或普通类型/必填/枚举格式错误可在剩余预算足够时重生成一次；重生成始终使用原始数据，不回传失败内容，不增加原始事实。网络/权限/限流/超时、越权额外字段、伪造证据不自动重试。
- 使用现有回写重试接口时，如上次模型失败，必须先重新完成模型分析，不能绕过此门槛。
- 前端显示完整“TAORAN六项检查：”，每项含检查状态和标准，未达标项附分析与修改建议；提交后反馈保留六项完整判断与模型理由。
- 模型名称、提示词版本、知识快照、原文引用、每次尝试的耗时及脱敏校验错误位置与结果一并保留在任务审计中；不记录密钥、失败响应原文或用户数据作为错误信息。

配置与真实验收见项目根目录`大模型接入方案.md`。未配置/未授权阶段继续运行本地规则，不能宣称真实模型已接通。

2026-08-26用户已确认“提交前快速规则检查、提交后GLM-5.2深度分析”，并使用TAORAN专用Key。本轮实际验收与启用状态见`智谱GLM-5.2接入验收记录.md`；不得将小样本调用成功视为生产稳定性已验证，不得自动改用其他型号或放宽证据、评分和响应时间约束。

提交前输出“检查结论、TAORAN六项检查、优先修改建议”，六项标准及真实检查状态全部显示，未达标项目另附具体分析和修改建议；不生成、不展示、不回写任何正式分数。提交后输出“Q33/Q34及总分、综合结论、六项判断、优先改进建议”。

### 提交后：深度评分与回写

```text
正式拜访记录及不可变首次提交时间
+ 客户类型和多商机阶段快照
+ 历史拜访、客户共识和下一行动证据
→ Q33确定性评分
→ Q34语义事实核验＋确定性评分
→ Q33/Q34综合100分
→ 形成AI意见和经理辅导建议
→ 通过简道云API回写指定AI字段
```

回写失败不覆盖评分结果，失败状态保留在审计记录中，并可使用回写重试接口再次执行。

## Q33：50分

### 信息完整性：25分

- 商机客户固定10项；
- 其他客户固定9项；
- 先计算必填字段完整率，再按90%、80%、70%、50%映射4、3、2、1、0档；
- 百分制分数=`档位分÷4×25`。

商机客户10项：客户类型II、当前商机阶段、预约、拜访目的、关键结果、过程、评价、下一行动时间、
下一行动目的、下一行动期望结果。其他客户不要求商机阶段。

### 24小时提交及时性：25分

结束基准依次取实际结束时间、开始时间＋时长、实际开始时间。不可变首次提交时间在结束基准后0—24小时内
为及时。单条记录及时得25分，不及时或证据不足得0分。

## Q34：50分

### 自评一致性：35分

规则和AI共同检查预约标准、KR具体可衡量且与目的一致、过程是否包含客观事实，由AI输出完全达到、部分达到、
未达到三分类，再与销售自评比较。一致得35分，不一致得0分。

### 下一行动：15分

下一行动目的和时间必须具体且晚于本次拜访：

- 商机客户：还必须有明确客户共识；
- 目标客户：下一次拜访不得与本次处于同一自然月；
- 潜力客户：下一次拜访不得与本次处于同一自然季度。

满足得15分，不满足得0分。

## 单条评分与周期评分边界

本Agent提交后回写的是单条拜访投影分：Q33和Q34各50分、总分100分。40题正式周期评分仍需先在测评周期内
汇总记录比例，再按原档位评分；不得把单条投影分的平均值直接当成Q40正式周期分。Q34正式Q40统计范围仍为面对面拜访，
非面对面记录可以获得TAORAN单条建议分，但标记为不进入Q40正式Q34汇总。

## 接口

- `POST /api/v1/connectors/jiandaoyun/visit/button-check`：AI检测按钮；
- `POST /api/v1/visit/checks`：标准提交前检查；
- `POST /api/v1/connectors/jiandaoyun/visit/evaluations`：提交后简道云原始字段评价；
- `POST /api/v1/visit/evaluations`：提交后标准评价；
- `GET /api/v1/visit/evaluations/{job_id}`：查询评价和回写结果；
- `POST /api/v1/visit/evaluations/{job_id}/writeback`：重试简道云回写。

未来Q40调用预留接口默认关闭，不改变现有40题评分：

- `GET /api/v1/integrations/q40/compatibility`：规则版本兼容检查；
- `GET /api/v1/integrations/q40/period-facts`：按员工和周期读取逐条结构化事实；
- `POST /api/v1/integrations/q40/evaluations:batch`：历史记录异步批量补评；
- `GET /api/v1/integrations/q40/batches/{batch_job_id}`：查询批次。

只有设置`DSM_TAORAN_ENABLE_Q40_INTEGRATION=true`且40题使用独立服务密钥主动调用时生效。

## 多租户配置边界

商业接入使用版本化租户注册表。每个租户独立配置启用状态、最多两个轮换访问Key、简道云API Key、Webhook Secret和
字段映射路径；按钮检查、提交后读取、签名Webhook、回写和字段同步必须按请求`tenant_id`解析同一租户配置，禁止回退到
其他租户。现有单租户环境变量只作为兼容回退，不修改TAORAN规则、Q33/Q34评分或默认关闭的40题接口。具体操作见
根目录`多租户配置与客户接入.md`。

## 禁止事项

- 不虚构客户反馈、客户共识、预算、时间、联系人、商机阶段或承诺；
- 不按客户名称连接核心实体；
- 不让大模型直接返回最终分；
- 不把提交前检查配置成表单阻断；
- 不用最后更新时间代替不可变首次提交时间；
- 不把Q33/Q34单条投影分冒充40题正式周期评分。
- 不在AI反馈意见、修改建议或经理可见文本中显示英文内部字段名。

## 当前简道云测试副本

当前只连接`拜访记录录入_AI测评`副本，不连接正式拜访记录表。应用ID、表单ID及两个AI字段ID已配置：“AI评分”
接收0—100总分，“AI反馈意见”接收Q33/Q34分项和规范建议。其他输入字段ID通过简道云V5字段接口按标题自动同步；
同步前先预览，显式使用`--apply`后才更新配置。

### 过程详细描述的按钮传参（2026-08-26已启用插件）

已核实测试副本原“自定义请求”事件漏传多行文本“过程详细描述”，不是用户未填或后端widget映射错误。
本地已准备`integrations/jiandaoyun/button_check.js`，通过简道云自建插件的文本入参转发本次草稿，保留换行；不读旧记录，不改原字段，不更改评分与后评流程。
接入、费用及验收清单见根目录`过程详细描述传参接入.md`及`简道云插件接入验收_20260826.md`。用户授权后已创建、启用插件并保存测试副本单个动作；平台调试及服务器审计确认正文换行、两类多行子表完整传递、清空重检及反馈回填。成员JSON数组提取稳定编号。空日期被平台完全省略时仍保守提示接口未获取，不将该边界误报为已解决。

### 0.6.5字段传递与七类修复（2026-08-26）

本节为0.6.5历史验收；当时云端插件待启用。当前启用情况以上一节及最新插件验收为准。
“其他”目的具体内容、三档自评证据、重复词拦截、总体结论、精确漏传列表、模型六项完整性和结论/事实一致性七类修复已完成。
提交后提示词升级至V2.2，六项未分析或指定事实矛盾时暂停正式回写；不把系统异常算作销售不合格。
239项测试、代码检查和构建通过，本地服务已启动0.6.5；公网能访问，但观察到一次非JSON响应，不能宣称稳定。
本轮未调用真实GLM、未改写简道云业务记录，不改Q33/Q34评分公式或40题项目。完整边界与后续验收见`字段传递与七类修复验收.md`。

### 0.7.0百分制评分（2026-08-26）

按用户最终更正，Q33和Q34各50分、总分100分；分项为25/25/35/15，题号与判断条件不变。新规则`TAORAN-Q33-Q34-100-V2`，历史200分结果只读保留，不自动重算、覆盖或向新量纲回写。接口、校准样例与Excel同步更新；40题项目及默认关闭的预留开关不变。详见`百分制评分调整与验收.md`。此前版本验收记录仅描述当时状态。

### 服务器隔离边界（2026-08-26）

用户确认新版服务器目录为`/TAORAN agent`，使用独立容器`dsm-taoran-v2-agent`、独立SQLite和本机8030端口。禁止覆盖旧`/opt/taoran-agent`、复用旧服务名、修改40题或知识库数据库、重启其他项目。独立HTTPS入口`https://taoran.yudaozhijian.top`的DNS、证书及公网接口验收已完成；简道云按钮及提交后通知尚未切换，不能宣称实际页面已迁移成功。只可维护本项目独立站点，不替换其他站点配置或证书。服务器部署、验收和待办以根目录`服务器独立部署.md`及`HTTPS接入验收_20260826.md`为准。
