# TAORAN智能体三反馈实施方案

版本：V1.0
日期：2026-08-27
目标版本：TAORAN智能体0.11.4
实施范围：简道云“拜访记录录入_AI测评”测试副本、TAORAN独立服务

## 一、实施目标

保留简道云现有一个“AI检测”按钮。销售人员点击一次后，由TAORAN服务同时生成并返回三份互相独立的提交前反馈：

1. **AI反馈意见（规则反馈）**：沿用原TAORAN本地六项规则，判断标准和反馈逻辑不调整。
2. **AI反馈意见（知识库反馈）**：每次点击实时调用DSM知识库API，取得当次知识内容后交给智谱大模型生成反馈；知识API失败时不使用本地快照冒充实时结果。
3. **AI反馈意见（大模型反馈）**：直接调用智谱大模型检查当前拜访记录，不读取或注入DSM知识库内容。

三份反馈均为提交前建议，不生成正式评分、不阻断提交。知识库或大模型分支异常时，只影响对应字段，规则反馈必须正常保留。

## 二、当前实施状态（2026-08-27）

- TAORAN智能体0.11.2代码、安装包和服务器镜像已完成并部署。
- 一个按钮同时生成三份反馈的后端接口已上线，公网合成数据验收三份反馈均非空。
- 简道云插件已改为一次请求、返回三个反馈结果，云端插件代码及返回参数已保存。
- 简道云31项字段已通过V5接口只读核验，匹配31项、未解析0项。
- 三个目标字段已确认：
  - `AI反馈意见（规则反馈）`：原反馈字段改名，字段ID保持不变。
  - `AI反馈意见（知识库反馈）`：新增字段。
  - `AI反馈意见（大模型反馈）`：新增字段。
- 自动化测试302项通过，代码检查和安装包构建通过。
- 简道云按钮动作已映射三个反馈字段；调试界面一次请求成功写入三份反馈，未提交正式业务记录。

## 三、上线前必须确认的业务边界

### 1. 提交后深度评价字段冲突

现有提交后评价流程继续把深度评价写入原`AI反馈意见`字段。该字段现已改名为`AI反馈意见（规则反馈）`，因此提交后深度评价会覆盖用户提交前刚取得的规则反馈。

业务方已于2026-08-27确认：该覆盖行为符合现行规则，无需新增“提交后深度评价”字段，也不调整原提交后回写流程。知识库反馈和大模型反馈字段不参与该覆盖。

### 2. 实施对象

第一阶段只在“拜访记录录入_AI测评”测试副本实施。完成真实页面验收后，再决定是否迁移到正式表单。

## 四、技术实施顺序

### 阶段一：部署前备份与基线记录

1. 记录当前TAORAN生产镜像版本、容器状态、健康状态和公网接口状态。
2. 备份服务器当前运行配置、字段映射和Compose配置；运行密钥文件只做服务器内受控备份，不下载、不打印。
3. 保存当前简道云插件代码、函数配置、按钮动作和输出字段映射截图。
4. 确认TAORAN没有正在执行的提交后评价任务。
5. 准备旧镜像和旧插件代码，作为快速回退材料。

验收标准：备份可识别、旧版本可恢复、其他DSM服务和40题项目未发生变化。

### 阶段二：服务器安全配置

1. 在服务器受控运行配置中增加DSM知识库API地址和知识库API Key。
2. 保留现有智谱模型地址、模型名称和TAORAN专用模型Key。
3. 知识库Key只写入服务器运行环境，文件权限保持600；不得写入代码、安装包、简道云插件、URL、日志或文档。
4. 确认模型超时、知识API超时、总体按钮响应预算和最大并发配置。
5. 配置完成后只检查“是否存在、是否可读取”，不输出Key内容。

验收标准：服务进程能够读取两类授权；安装包和日志中不存在真实密钥。

### 阶段三：部署TAORAN 0.11.2服务

1. 使用0.11.2完整源码部署包构建独立新镜像，镜像名称带明确版本号和日期。
2. 保留旧镜像，不覆盖历史发布目录。
3. 先在服务器内部启动新版本并执行健康检查。
4. 校验服务版本、数据库挂载、字段映射路径和租户配置。
5. 确认健康后，再由现有独立HTTPS入口转发到0.11.2服务。

验收标准：健康接口返回正常；容器无持续重启；原数据库和租户配置可正常读取；40题、知识库及其他容器状态不变。

### 阶段四：后端三反馈接口验收

在尚未更新简道云插件前，使用虚构拜访数据直接验证后端接口：

1. 一次请求必须同时返回`rule_feedback_text`、`knowledge_feedback_text`和`model_feedback_text`。
2. 兼容字段`feedback_text`必须与`rule_feedback_text`完全一致。
3. 规则反馈与旧版本相同输入的结果进行回归对比，确认规则没有改变。
4. 知识库反馈必须成功取得本次实时知识快照，并由智谱生成反馈。
5. 大模型反馈请求中不得包含知识库条目或知识库正文。
6. 人为模拟知识API失败，确认只显示知识库反馈未完成，另外两份反馈仍返回。
7. 人为模拟模型失败或超时，确认规则反馈仍返回，不影响提交。
8. 测量端到端响应时间；目标是在简道云插件12秒超时限制内完成。

验收标准：三字段完整、分支隔离、无正式评分、无密钥泄露、响应时间符合按钮要求。

### 阶段五：更新简道云插件和字段映射

后端验收通过后，才更新简道云，避免云端插件先切换后无法识别旧服务响应。

1. 将0.11.2的`integrations/jiandaoyun/button_check.js`更新到原插件函数。
2. 保留原“TAORAN AI检测”一个按钮，不新增第二或第三个按钮。
3. 保留原有19项正文/关联字段和2个子表输入绑定，不改变用户填报方式。
4. 配置三个输出：
   - `rule_feedback_text` → `AI反馈意见（规则反馈）`
   - `knowledge_feedback_text` → `AI反馈意见（知识库反馈）`
   - `model_feedback_text` → `AI反馈意见（大模型反馈）`
5. `feedback_text`仅作为旧规则反馈兼容输出，不再单独绑定其他字段。
6. 不把`check_status`等调试字段绑定到业务反馈字段，不绑定或修改`AI评分`。
7. 保存后重新加载设计器，确认插件代码、参数和映射持久存在。

验收标准：页面仍只有一个AI检测按钮；点击后三个字段分别写入对应反馈，无串写、漏写或旧意见残留。

### 阶段六：真实页面用户验收

只使用测试副本和专用测试草稿，至少完成以下场景：

| 场景 | 预期结果 |
| --- | --- |
| 字段完整且事实清晰 | 三份反馈都返回，规则反馈与原逻辑一致 |
| 关键结果空泛 | 三份反馈分别指出问题，不能互相复制冒充 |
| 过程描述只有主观判断 | 规则和模型能够提示缺少客户事实 |
| 缺少下一步行动或日期 | 对应TAORAN N项给出完善建议 |
| 知识API临时不可用 | 知识库字段明确显示未完成，规则和大模型字段正常 |
| 智谱模型超时 | 对应模型分支显示未完成，规则反馈正常，表单仍可提交 |
| 连续修改后重复检测 | 三个字段都更新为本次结果，不保留上次旧意见 |

验收过程中不点击正式提交、不修改已有真实业务记录，除非另行批准提交后回写测试。

### 阶段七：观察与正式切换

1. 测试副本通过后观察至少一个完整业务日。
2. 记录按钮成功率、平均及最大响应时间、知识API失败率、模型超时率和用户反馈。
3. 日志只记录请求ID、分支状态、耗时和知识快照Hash，不记录密钥、完整请求头或完整客户原文。
4. 达到验收指标后，再评估正式表单切换；正式切换继续采用“后端先部署、简道云后切换”的顺序。

建议上线指标：

- 规则反馈成功率：100%；
- 三反馈整体成功率：不低于95%；
- 正常情况下按钮响应时间：不超过12秒；
- 字段串写、规则变化、正式评分误写、密钥泄露：0次。

## 五、回退方案

出现以下任一情况应立即回退：规则反馈与旧逻辑明显不一致、按钮大量超时、三个字段串写、正式评分被提交前流程修改、真实密钥出现在日志、服务影响其他DSM项目。

回退步骤：

1. 先把简道云插件恢复到备份版本和原输出映射，停止新三字段请求。
2. 将TAORAN服务切回旧镜像和旧发布目录。
3. 执行健康检查和原单反馈接口检查。
4. 保留0.11.2故障日志和审计记录，不删除业务数据。
5. 仅回退本项目容器，不重启或清理其他DSM、知识库和40题服务。

知识库Key可保留在服务器受控配置中，也可按安全要求撤销并更换；不得在回退过程中打印或复制到普通文件。

## 六、职责建议

| 角色 | 主要职责 |
| --- | --- |
| TAORAN技术实施 | 服务部署、密钥配置、接口验证、监控和回退 |
| 简道云管理员 | 插件更新、按钮参数及三个输出字段映射 |
| 业务验收人员 | 判断三份反馈是否符合销售拜访填写规范 |
| 安全/系统管理员 | 密钥保管、服务器权限和日志检查 |

## 七、预计实施时间

在服务器、简道云管理权限及业务验收人员均可用的情况下：

- 备份和上线前确认：30分钟；
- 服务器配置、部署和接口验收：60—90分钟；
- 简道云插件更新和真实页面测试：45—60分钟；
- 回归、记录和交付：30分钟；
- 业务观察：至少1个完整业务日。

首次技术实施预计约3小时，不包含业务观察期和新增“提交后深度评价”字段所需的业务确认时间。

## 八、最终交付物

1. TAORAN智能体0.11.2服务器镜像及版本记录；
2. 服务器受控运行配置检查记录，不包含真实密钥；
3. 简道云三反馈插件代码和三个字段映射截图；
4. 后端接口、页面端到端和异常分支验收记录；
5. 回退材料及回退验证记录；
6. 正式表单是否切换的业务确认结论。

## 九、实施与验收记录（2026-08-27）

| 项目 | 实施结果 |
| --- | --- |
| 服务器版本 | `dsm-taoran-v2:0.11.2-20260827`已运行，容器健康、重启计数0；`current`指向`releases/0.11.2-20260827`。 |
| 自动化回归 | 302项通过；保留1条已知Starlette弃用提示。 |
| 后端公网验收 | 一次请求返回规则、实时知识库和纯大模型三份非空反馈；兼容输出与规则反馈一致；实测约9.25秒。 |
| 知识库分支 | 使用服务器受控Key实时调用DSM知识库API；取得2条引用和知识快照Hash；不使用本地快照回退。 |
| 简道云插件 | 云端代码和三个返回参数已保存；保留原一个“AI检测”按钮。 |
| 字段映射 | 兼容规则反馈→`AI反馈意见（规则反馈）`；知识库反馈→`AI反馈意见（知识库反馈）`；大模型反馈→`AI反馈意见（大模型反馈）`。 |
| 页面联调 | 使用前端事件调试发送一次空白测试数据，三个目标字段均自动填入对应反馈；未提交或新增正式拜访记录。 |
| 回退材料 | 旧镜像、旧Compose配置、旧运行配置和旧字段映射均保留在服务器。 |

当前技术部署与简道云三字段联调已完成。后续只需按阶段七观察一个完整业务日，并由业务人员抽查真实填写质量；提交后深度评价覆盖规则反馈的行为按业务确认继续保留。

### 实际测试修复补充

实际业务测试发现前端动作仅持久化了旧规则反馈映射，且提交后回写没有生成两个新增字段的值。现已升级到0.11.4：用最新插件定义重建唯一的三反馈动作，并在关闭动作面板后保存表单；提交后也会独立生成、回写知识库和纯大模型反馈，历史任务重试时会补生成缺失值。全新页面已确认三项映射持久存在，当前动作联调已一次填充3个反馈字段；原失败测试记录受控重试后，AI评分及三个反馈字段全部回写成功。
