# 弹窗最终反馈展示调整（未部署）

基于 origin/main 578e666，在独立分支 codex/popup-final-label-20260908 调整。等待后续调整完成后统一发布。

- 弹窗栏目标题使用“AI最终反馈意见”。
- 显示最终正文时，仅移除开头重复的“【AI反馈意见】”；正文中同名引用保留。接口原文及“已读并返回”交付内容保持原样。
- 实时分析成功、最终意见尚未完成时，展开最终意见区域，显示“正在生成最终AI反馈意见”，状态提示保持一致。最终意见成功后替换为正文，失败后移除等待提示并显示原失败状态。
- 重新打开、实时分析先完成、最终意见先完成、实时分析不可用、记录版本失效等分支继续沿用现有任务保护。
- 将“分析完成后，请点击已读并返回……”提示移至标题正下方，再显示任务状态；删除页面数据来源说明和运行提示中“可关闭后重新打开查看”等措辞。保留直接关闭不回填的说明，以及失败/过期时必要的重新检测操作指引。

验证：438 项 Python 测试、48 项页面交互自动化测试、Ruff、差异格式检查、Compose 配置校验通过。本地 Docker 引擎未运行，未进行镜像构建；未操作服务器、未部署、未合并远端 main。
