# V4.7 当前实施口径

版本：0.27.5rc2。此文覆盖旧文档中与本次用户确认相冲突的语义拦截、重复点击及双模型预览策略。

## 意见与评分

- 主体、目标、引用归属、意见一致性等语义检查只记录后台观察，不拒绝意见、不触发语义重生成，也不据此暂停正式评分。
- 后台使用本次最新提交的结构化模型判断计算评分。Q33/Q34 权重和确定性评分规则不变。基础检查不产生正式评分。
- 原文证据从当前输入中由程序提取，模型只选择来源 ID；无法解析的来源 ID 记录观察，不展示虚构引用。
- 不向模型提供历史 AI 意见作为事实。按原定目标逐项解释，区分已记录、无法确认及未来建议，允许不足以判断。
- 严格输出格式、访问权限、确定性的日期规则和旧版本回写保护仍有效。

## 弹窗和任务

- 创建任务时即持久保存基础检查、规范化记录快照、输入版本及任务访问凭证，默认保留 7 天。
- 弹窗首次 HTML 就包含“基础检查（程序生成，非 AI 分析，不代表正式评分）”，无需等待模型或第二次网络请求才有内容。
- 单次 AI 生成在后台运行；浏览器请求超时、断线、关闭，不会取消任务。完成后原页面自动查询并显示对应版本的“AI实时分析”。
- 同一用户、记录及输入版本重复点击复用任务，包括 force 请求；重启后也通过持久记录找回。失败任务通过“恢复本次分析”沿用同一任务 ID 和输入快照。
- 网络超时/断网、408/429/5xx 最多额外重试两次；格式错误最多修复一次；总尝试上限四次。持续故障进入可恢复状态，不假称完成，不无限调用。
- 不再并行调用第二个模型生成预览，减少重复生成、费用和排队。生成耗时按模型排队、首字等待、正文生成分别记录。
- 结果携带输入版本和生成时间；不同版本响应不替换当前页面，新版本产生后旧任务不能被确认回传。基础检查不能通过确认接口冒充 AI 意见。

## 可观测与安全边界

模型解释仍可能产生语义偏差；本版按用户决定允许展示并计分，将观察结果与原文引用保留用于复盘，不声称语义观察通过或业务准确率为 100%。临时故障恢复与正式提交回写是不同链路，恢复弹窗分析不自动提交或保存简道云记录。

持久快照和模型观察只写入应用私有数据目录，公开接口不返回密钥、会话凭证或完整请求快照。旧版本、映射、知识策略和业务记录变更仍由原有回写保护处理。
