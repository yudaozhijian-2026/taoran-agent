# 过程详细描述传参接入

> 2026-08-26更新：当前Q33/Q34各50分、合计100分，程序0.7.0。云端插件已创建、启用并保存到测试副本；最新验收见《简道云插件接入验收_20260826》。旧版本及旧测试数量为历史记录。

## 当前状态（2026-08-26）

**用户确认已充值并授权插件调整后，已启用“TAORAN拜访草稿检查”，测试副本原有单个“TAORAN AI检测”动作已改为插件；19项文本和2项子表已绑定。平台前端事件调试确认多行正文、两行联系人、两行商机完整传递，清空正文后R项变为未达标，意见正常回填。**

现用服务地址：`https://taoran.yudaozhijian.top/api/v1/connectors/jiandaoyun/visit/button-check`。原认证移至插件通用参数，仍仅回填“AI反馈意见”；不再使用原自定义请求Body。空日期可能被平台完全省略，当前保守提示“接口未获取”，此边界仍待完善。

只针对 `拜访记录录入_AI测评`：应用 `60fe7ad79ca2d000075dfab1`，表单 `6a8408b7c5a0d9454090a5bc`。
字段传递修复不修改正式表、历史拜访内容、提交后Webhook或40题项目。当前本地服务已升级0.7.0，评分按用户另行确认调整为Q33/Q34各50分；原0.6.5七类修复与验收见`字段传递与七类修复验收.md`。

## 已确认的原因

- 原字段为“多行文本”：`过程详细描述` / `_widget_1574484940277`，后端映射到 `process_description` 正确。
- 浏览器检查实际“TAORAN AI检测”事件：Body含租户及18个输入字段，没有过程字段；插入字段列表搜索“过程”无结果。
- 不是“销售没有填写”，而是当前“自定义请求”不支持引用多行文本作为Body值。[简道云自定义请求说明](https://hc.jiandaoyun.com/open/12206)的2.2.3明确Body可插入字段同URL字段列表，列表没有多行文本。
- “调用插件”的文本入参支持多行字段值，包含换行。[官方代码设计：请求参数储值格式](https://hc.jiandaoyun.com/open/23402)。

不将原字段改成单行文本，不用已保存记录替代未提交草稿，不添加可能丢失换行的隐藏镜像字段。

## 已获授权并完成的简道云变更

新增企业内部自建插件 `TAORAN拜访草稿检查`，后端函数显示名 `拜访草稿TAORAN检查`（Node.js 20）；只做请求转发及失败提示，不运行大模型。
将测试副本现有“AI检测”的执行动作改为该插件，保留原有输入绑定并增加过程字段、联系人与商机子表；后两类须先验证页面可绑定的完整行结构。返回值只填入“AI反馈意见”。

现场只读确认云币100.00、插件云币支付开关原本已开启。当前页面计价提示为基础执行10000次1云币、第三方依赖10000次2云币；使用Node.js 20内置axios，未上传自定义依赖。具体结算以平台账单为准。本次未充值、未修改企业支付开关、未接受新增协议。[官方付费说明](https://hc.jiandaoyun.com/open/23483)。

## 部署配置清单

### 1. 通用参数（仅管理员配置）

| 参数ID | 类型 | 配置 |
|---|---|---|
| `endpoint_url` | 文本 | 当前可用的HTTPS入口＋`/api/v1/connectors/jiandaoyun/visit/button-check` |
| `tenant_id` | 文本 | `tenant_demo` |
| `api_key` | 文本 | TAORAN服务密钥，沿用现有按钮的服务鉴权；不是智谱、知识库或简道云管理API Key |

密钥只放在插件受控配置中，不放请求URL、表单字段、代码、文档、截图或调试输出。
已停用旧临时入口，不将临时域名用于当前插件。

### 2. 请求参数

下表19项声明为插件的“文本”入参，使用 **字段值** 绑定，不手填字段名称或widget ID字符串，不使用拼接JSON模板。
过程参数必须配置字段绑定，但不得设置“内容不能为空”的插件前置校验；用户清空时应让Agent返回缺失建议。
另有两项声明为真正“子表单”类型，总计21项输入；已现场使用“按子表单赋值”绑定，并核对两行数据在服务器中的对应关系。

| 参数ID | 测试副本字段 |
|---|---|
| `visit_date` | 拜访日期 |
| `employee_id` | 销售代表（通讯录）；插件从JSON成员数组提取稳定编号，不发送姓名 |
| `customer_id` | 客户编号 |
| `customer_type_ii` | 客户分类II |
| `visit_method` | 拜访方式 |
| `is_appointment` | 是否预约 |
| `purpose_code` | 拜访目的 |
| `other_purpose` | 具体其他目的 |
| `expected_key_result` | 想取得的关键结果 |
| **`process_description`** | **过程详细描述 / `_widget_1574484940277`** |
| `self_assessment` | 评价 |
| `next_action_purpose` | 下一次行动目的 |
| `next_action_other_purpose` | 下一次具体其他目的 |
| `next_action_expected_result` | 下次拜访期望的关键结果 |
| `next_contact_at` | 下一次联系客户时间安排 |
| `actual_start_at` | 实际拜访开始时间 |
| `actual_end_at` | 实际拜访结束时间 |
| `duration_minutes` | 拜访时长 |
| `evidence_ids` | 上传相关文件 |

#### 两类子表的最小传递内容

| 参数ID | 原子表与子字段 | 数据范围 |
|---|---|---|
| `participants` | 联系人子表`_widget_1574484940098`；关联主键`_widget_1416718540131` | 每行仅联系人稳定编号，不发姓名、手机或邮箱。 |
| `opportunities` | 商机子表`_widget_1785314290795`；商机编号`_widget_1785314290802`、历史阶段`_widget_1785314290798`、最新阶段`_widget_1785314290799` | 保留同一行的编号与阶段对应。 |

组件支持数组或JSON数组字符串。当前云端为“按子表单赋值”，各子字段分别绑定稳定编号、历史阶段及最新阶段；不拼接列数组。
若平台不能提供完整行结构，停止该绑定并报告，不以旧记录或丢失行关系的文本替代。
清空为收到空数组，完全漏传保持未收到；错误容器、非对象行或非空行没有可映射字段返回中文检测未完成提示。
本次联系人不代表已核实下一次对象；角色、下一次对象的独立来源仍待公司确认，不虚构widget ID。

### 3. 函数代码及返回值

代码：`integrations/jiandaoyun/button_check.js`（插件代码编辑器中的函数体，不能直接作为普通Node命令运行）。
返回参数 `feedback_text`、`check_status` 在平台声明为any，函数实际均返回字符串；仅将 `feedback_text` 绑定到“AI反馈意见” / `_widget_1787037882560`。
**不绑定“AI评分”**。不要并行保留两个向同一意见字段赋值的执行动作，以免旧响应覆盖新响应；旧动作参数按本清单及原配置保留，必要时恢复。

组件约束：

- 每次点击使用当前入参，后端生成新的检查ID；不缓存旧意见、不读取历史记录、不提交记录。
- 多行原文在转发过程中不截断、不去空格、不替换换行；Agent现有规范化仍可能去除首尾空白。
- 未收到过程参数时返回明确配置问题；收到空串或null则发送空内容供规则检查。注意平台未绑定时若生成null，仍须通过现场绑定检查区分，不能仅靠运行值断言绑定正确。
- 超时设置12秒，无自动重试；禁止HTTP跳转，避免鉴权头泄漏到跳转地址；请求/响应超过256KiB不截断后继续判断，而是返回本次检测未完成。
- 网络、接口或响应格式失败时回填本次失败说明，不静默保留旧意见；不显示密钥、原始异常、知识依据、分析方式或分数。
- 插件执行失败回填说明属于正常返回，可能仍计一次成功插件调用费用。

## 本地测试与线上验收边界

自动测试使用合成数据和替身网络，无真实客户内容或简道云云币消耗。另以合成数据调用本地和既有HTTPS入口，只写本地检查审计，不提交简道云业务记录。

```bash
uv run pytest tests/test_button_plugin.py tests/test_api.py -q
```

验证了：多行/引号/反斜杠/中文/emoji/长文本无损转发；未绑定与主动清空分别处理；当前内容→修改为观点→清空→补齐事实的重复检测；R项真实检查而非“未检查”；自评检查接收过程事实；六项标准继续展示；不生成提交前正式评分；错误配置、附件格式、超时/连接失败、错误响应均返回中文意见。

0.6.5新增两类子表多行、JSON数组字符串、修改阶段、清空、漏传及错误格式验证；通过插件替身→真实本地接口→审计输入核对传递结果。全项目239项测试通过，但不等于云端字段已收到。

原计划的线上验收清单如下；实际已完成及剩余边界以《简道云插件接入验收_20260826》为准：

1. 确认后新建并启用内部插件，核验21项输入的实际绑定能力；保存测试副本按钮输入及意见输出绑定。不自动充值或开启支付。
2. 仅在新建记录预览/调试页面填合成数据，**不提交拜访记录**；不调用真实客户记录。
3. 写两行客观事实，点击AI检测；在本地审计中核对 `source_supplied_fields` 含过程字段、文本与本次内容相符，R项不再“未检查”。
4. 改为纯观点、再清空、再补齐，分别点击；确认意见随内容变化、请求ID不同且没有沿用旧内容。
5. 核对联系人编号及商机编号/阶段的逐行对应；修改、清空子表后再检，确认当前草稿同步生效。
6. 核对AI评分未被填写、原始字段未被改写、六项标准保留；记录实际耗时。
7. 保存验收结果及最终入口；遇到故障可恢复旧事件，但旧事件仍存在多行漏传问题，须如实标记。

本次没有修改或验证提交后回写成功率，该链路另行验收。
